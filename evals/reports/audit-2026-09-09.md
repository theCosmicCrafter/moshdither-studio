# Release-readiness audit — 2026-09-09

Produced by a seven-dimension read-only audit with adversarial verification
(113 agents planned; 59 completed before the run exhausted usage credits, so
52 serious findings carry an is-it-real / does-it-block verdict and the rest
carry evidence only). Every finding below names the file and line it was found
at, so it can be re-checked rather than taken on trust.

Dimensions: recorded ledger, hidden/half-wired UI, crashes and unhandled
failure, packaging and release mechanics, what the gates actually prove, a
stranger's first run, and data safety / security / licensing.

**106 distinct findings** — blocker: 11, major: 41, minor: 46, unknown: 8

---

## BLOCKER (11)

### Installer is unsigned: SmartScreen 'Windows protected your PC' is the stranger's first screen

- **Area:** Install  
- **Effort:** human-step · **User-facing:** yes

**Why it matters.** For a non-technical audience an unsigned NSIS/MSI is frequently abandoned at the SmartScreen dialog; nothing in the app can fix this, it needs an Authenticode certificate.

**Evidence.**

```
src-tauri/tauri.conf.json:82-83 `"certificateThumbprint": null, "signCommand": null`. README.md:97-101 acknowledges: "Releases are **unsigned**, so Windows SmartScreen will warn on first run".
```

### SAM3 cannot work on a machine without an NVIDIA GPU, and nothing tells the user before they download 6.5 GB

- **Area:** SAM3 / first run without CUDA  
- **Effort:** large · **User-facing:** yes

**Why it matters.** A stranger on a laptop with integrated graphics accepts the licence, downloads 6.5 GB, waits through a 4.8 GB temp unpack, and every prompt then fails with a PyTorch dtype error. That is the product's headline AI feature dead on arrival for most non-technical Windows users, with the discovery happening after the most expensive step.

**Evidence.**

```
git log -1 5540c3f (2026-09-08, the ONLY commit that ever touched the CPU shim; `git log -S'_InertAutocast' -- src-tauri/sam3_bridge.py` returns just this one): "INCOMPLETE — CPU fallback ... IT DOES NOT WORK YET. The model now builds and loads on CPU, then set_image fails with 'mat1 and mat2 must have the same dtype, but got BFloat16 and Float'. That is where this stops; do not read the shim as a working CPU mode." The shim is still what ships: src-tauri/sam3_bridge.py:167-231 (`if not torch.cuda.is_available():` monkeypatches). The sidecar is deliberately a CUDA build: scripts/build-sam3-sidecar.py:119-152 refuses to freeze a CPU torch. Error reaches the user verbatim: sam3_bridge.py:1082-1090 returns `str(e)` as the response message, MaskPanel.tsx:124 shows `SAM3 auto-load failed: ${e}`. No GPU requirement is stated anywhere the user reads: `grep -n -i "gpu|nvidia|cuda" README.md` -> no hits; Sam3Setup.tsx:104-119 panel text mentions only size. The bridge's CPU warning (sam3_bridge.py:227-231) goes to stderr, which sam3_engine.rs:453 sends to the log file only.
```

### SAM3 add-on manifest URL returns 404 today; the offline/unpublished fallback text tells end users to run npm

- **Area:** SAM3 add-on download  
- **Effort:** human-step · **User-facing:** yes

**Why it matters.** Until the ~6.5 GB upload happens (a known human step) every installed copy's SAM3 button is a dead end, and the message it dies with is a developer build instruction. The offline branch will keep showing that instruction to non-technical users even after publication.

**Evidence.**

```
`gh release list --limit 10` -> empty; `gh release view sam3-addon-v1` -> "release not found". sam3_addon.rs:45 hard-codes MANIFEST_URL to that tag. A 404 surfaces as sam3_addon.rs:330-331 `SAM3 asset list unavailable: {e}` (reqwest's "HTTP status client error (404 Not Found) for url (...)"). With no internet the user sees sam3_addon.rs:325-328: "Could not reach the SAM3 asset list at {url}: {e}\n\nIf this release has not published the add-on yet, build the sidecar locally with `npm run build:sam3-sidecar` instead." Sam3Setup.tsx:175-179 renders that string raw in the panel.
```

### Still-image export clones the frame duration x fps times with no cap -- a phone photo at default settings needs ~15-30 GB and aborts the process

- **Area:** export / memory  
- **Effort:** small · **User-facing:** yes

**Why it matters.** Exporting a photo with default settings is the first thing a new user does. On any consumer machine this is an out-of-memory abort with nothing in the log after "Decoded 1 frames" -- exactly the shape of the unreproduced export crash the owner is chasing.

**Evidence.**

```
src-tauri/src/commands.rs:868-877: `if segment.frames.len() == 1 { let effective_fps = fps.unwrap_or(30.0); let target_duration = trim_end.unwrap_or(10.0); let target_frames = (target_duration * effective_fps).round() as usize; ... segment.frames = vec![single; target_frames]; }` -- `Frame` owns a `Vec<u8>`, so this is a deep copy per frame. The frontend always sends a trim end: src/components/ExportPanel/index.tsx:254 `const trimEnd = typeof outPoint === "number" ? outPoint : state.duration;` and src/store/index.ts:667 `setDuration: (d) => set({ duration: Math.max(0.1, d) })` / src/components/Timeline/index.tsx:275 clamp to >= 0.1 only -- no upper bound. The memory plan never sees this: src-tauri/src/ffmpeg/mod.rs:101-109 `probe_frame_count` returns 0 for a still (no duration) and :129 `.max(1)` makes it 1 frame, which fits, so no downscale; `fit_to_preview_budget` (commands.rs:100-121) only applies to the preview frame, not to the export decode. Numbers: 4032x3024x4 = 48.8 MB per frame x 300 frames (default 10 s x 30 fps) = 14.6 GB, then the per-effect rayon `.collect()` (commands.rs:966-981) doubles it. Typing 120 in the strip's duration box = 175 GB. A failed Rust allocation is not a panic: it aborts without reaching the panic hook or the log (crash.rs:181-196 only sees panics).
```

### Export silently renders long clips at a fraction of their resolution and truncates anything over ~4.5 minutes; the only warning the backend emits is dropped by the UI

- **Area:** export / memory budget  
- **Effort:** large · **User-facing:** yes

**Why it matters.** The product's headline use is moshing a music video. A 3-minute 1080p clip is decoded at 480x270, effects run at that size, and the result is upscaled to 1080p with no message; a 5-minute clip comes back shorter. The user sees a blurry, cut export and has no way to know why -- the one warning that exists never reaches the screen.

**Evidence.**

```
src-tauri/src/ffmpeg/mod.rs:15 `DEFAULT_DECODE_MAX_FRAMES: usize = 10_000`; :22 `MAX_DECODE_MEMORY_BUDGET_BYTES = 4 GiB`; :35 `DECODE_MEMORY_FRACTION_OF_RAM = 0.50` of *available* RAM; :69-99 `pick_auto_scale` walks 3840→480 and ends `Some(480)` with the comment "let the caller truncate"; :460 `requested_max = max_frames.unwrap_or(DEFAULT_DECODE_MAX_FRAMES)`; :572-575 decode loop `if frames.len() >= max_frames { break; }`; :590-600 the truncation notice is `tracing::warn!` only. src-tauri/src/commands.rs:838 export passes `None` for max_frames; :850-857 the downscale notice is emitted as `"warning": message, "downscaled_to": s`; :858-863 the post-decode 95%-of-budget check is `tracing::warn!` only. src/components/ExportPanel/index.tsx:216-236 the `export-progress` listener destructures only `{ stage, progress, message }` -- `grep -n 'warning\|downscaled_to' src/components/ExportPanel/index.tsx` returns nothing. Output is then upscaled back to the target size by `-vf scale` (ffmpeg/mod.rs:1207-1235). Arithmetic at 1080p/30fps against the 4 GiB cap: native 8.29 MB/frame holds 518 frames (17 s); 1440 rung 31 s; 1280 rung 39 s; 1080 rung 54 s; 720x405 123 s; 480x270 276 s; beyond 4:36 the clip is cut. Independently, the 10,000-frame ceiling cuts any clip at 5:33 (30 fps) or 2:47 (60 fps). :550 `DECODE_TIMEOUT = 120 s` additionally fails a slow decode with `Process timed out after 120s` (:1519-1522).
```

### 35 bundled LUTs have no provenance or licence on record; the conversion script names commercial and unknown-origin packs

- **Area:** licensing / bundled assets  
- **Effort:** human-step · **User-facing:** no

**Why it matters.** The installer redistributes third-party creative assets, at least one from a commercial vendor, with no licence or attribution; for a public release this is direct infringement exposure and the fix is to confirm each pack's redistribution terms or drop the file.

**Evidence.**

```
scripts/convert_curated_luts.py:15 `LUT_ROOT = Path(r"D:\models\LUTs")`, lines 20-47 list sources including `Kodak\Dehancer-Rec.709-Kodak_Vision3_250D-33.cube` (Dehancer is a commercial film-emulation product), `Movie-Looks\28 Days Later - LOG.cube`, `300 - LOG.cube`, `3h10 to Yuma - LOG.cube`, `Sci-Fi-Futuristic\Back To The Future 3 - LOG.cube`, `TPF - Cinematica 1 - LOG.CUBE`, `Hollywood Tones LUT #1.cube`, `Vintage\Action_Movie_Free_LM (1).cube`. `ls public/lut` = 35 PNGs (amatorka, brannan, earlybird, etikate, gotham, hefe, inkwell, kelvin, lofi, lookup, nashville, sutro, toaster, walden, xpro match the GPUImage lookup-filter set by name; no attribution present). `ls public/lut/*.md LICENSE* README*` -> none. `git grep -i dehancer|lut pack|rocketstock|lutify` across docs/README/THIRD-PARTY-NOTICES -> only the script path above. Bundled by src-tauri/tauri.conf.json:66 `"../public/lut": "lut"`.
```

### GPL corresponding source is not published and no GitHub release exists at all; the SAM3 add-on manifest URL points at a release tag that does not exist

- **Area:** licensing / release  
- **Effort:** human-step · **User-facing:** yes

**Why it matters.** Shipping ffmpeg/ffprobe (GPLv3) and ffgac/ffedit (GPLv2+) without the offered source alongside the installer breaks the licence on day one; until the sam3-addon-v1 release exists the in-app SAM3 installer fails with "Could not reach the SAM3 asset list" (sam3_addon.rs:289-296). Both are human steps the checklist already names.

**Evidence.**

```
`gh release list --repo theCosmicCrafter/moshdither-studio --limit 10` -> empty output, exit=0. src-tauri/src/sam3_addon.rs:45 `const MANIFEST_URL: &str = "https://github.com/theCosmicCrafter/moshdither-studio/releases/download/sam3-addon-v1/sam3-assets.json";`. THIRD-PARTY-NOTICES.md:130-145 "Release checklist (GPL corresponding source) ... Attach both to the same release as the installer"; docs/SESSION-HANDOFF.md:161-163 "GPL corresponding source. Attach the two tarballs ... This is a real obligation". git log master..HEAD = 112 unmerged commits on this branch.
```

### SAM3 add-on release 'sam3-addon-v1' does not exist on GitHub; the repo has zero releases, so the in-app SAM3 installer 404s for every user

- **Area:** packaging/release — SAM3 add-on  
- **Effort:** human-step · **User-facing:** yes

**Why it matters.** AI segmentation is a headline feature (README:14). Any public user who clicks "Download and install SAM3" (src/components/Sam3Setup.tsx:153) gets an HTTP 404 error string. Until the ~6 GB upload happens, the shipped product's SAM3 feature is dead on arrival.

**Evidence.**

```
`gh release view sam3-addon-v1 --repo theCosmicCrafter/moshdither-studio` -> "release not found" (exit 1). `gh release list --repo theCosmicCrafter/moshdither-studio --limit 20` -> empty output (exit 0). src-tauri/src/sam3_addon.rs:45 `const MANIFEST_URL: &str = "https://github.com/theCosmicCrafter/moshdither-studio/releases/download/sam3-addon-v1/sam3-assets.json"`. The 0.2.0 installer carries no sidecar: `7z l "src-tauri/target/release/bundle/nsis/MoshDither Studio_0.2.0_x64-setup.exe"` lists exes moshdither-studio.exe, ffedit.exe, ffgac.exe, ffmpeg.exe, ffprobe.exe, mosh-cli.exe, mosh-verify.exe, uninstall.exe — no sam3-bridge.exe. sam3_engine.rs:164-168 documents why it cannot ship (WiX LGHT0263 2 GiB limit, NSIS mmap failure). On the 404 path a user sees sam3_addon.rs:330-331 `"SAM3 asset list unavailable: {e}"` (reqwest error_for_status). Assets are staged locally only: build-archive/sam3-addon/ has sam3-assets.json, sam3-bridge.part0 (1,900,000,000 B), sam3-bridge.part1 (1,161,917,014 B), sam3.part0, sam3.part1, all dated Sep 8 23:02; the staged manifest's sidecar sha256 a1066072deda1506ad47a540170e00257ab7c3898e34d6bc44798782313dec9b equals EXPECTED_SIDECAR_SHA256 at sam3_addon.rs:51-52.
```

### No GitHub release exists and the SAM3 add-on manifest URL 404s -- segmentation is dead for every public user until the ~6 GB upload happens

- **Area:** release / SAM3 add-on  
- **Effort:** human-step · **User-facing:** yes

**Why it matters.** The Mask panel offers a 6 GB download that cannot succeed. Handoff line 157-160 and PRODUCT goal both treat SAM3 as a shipped feature.

**Evidence.**

```
gh auth status -> 'Logged in to github.com account theCosmicCrafter'; gh api repos/theCosmicCrafter/moshdither-studio/releases --jq length -> 0; gh api .../releases/tags/sam3-addon-v1 -> HTTP 404. src-tauri/src/sam3_addon.rs:45 MANIFEST_URL = https://github.com/theCosmicCrafter/moshdither-studio/releases/download/sam3-addon-v1/sam3-assets.json. Staged but unuploaded assets: build-archive/sam3-addon/ (sam3-bridge.part0 1,900,000,000 B, sam3-bridge.part1 1,161,917,014 B, sam3.part0 1,900,000,000 B, sam3.part1 1,550,062,241 B, sam3-assets.json); scratchpad/publish.log: '[sam3-addon] staged only. Re-run with --upload to publish'. Hash side IS done: sam3_addon.rs:51-52 EXPECTED_SIDECAR_SHA256 = a1066072deda1506ad47a540170e00257ab7c3898e34d6bc44798782313dec9b == build-archive/sam3-addon/sam3-assets.json sidecar.sha256 == scratchpad/sidecar.sha256.
```

### 112 commits ahead of master, 93 not even pushed; PR #49's title describes none of it

- **Area:** release / git  
- **Effort:** human-step · **User-facing:** no

**Why it matters.** Nothing public reflects the tree that was audited; a machine loss would lose 93 commits of work.

**Evidence.**

```
git rev-list --count master..HEAD -> 112; git rev-list --count origin/Cosmic/upbeat-golick-68081b..HEAD -> 93; git diff --stat origin/Cosmic/upbeat-golick-68081b..HEAD -> '202 files changed, 19580 insertions(+), 1350 deletions(-)'; git ls-remote --heads origin -> branch at f8a8d08 vs local HEAD 274502f. gh pr list -> '#49 fix(ui): repair silently-broken design tokens and unify UI inconsistencies  Cosmic/upbeat-golick-68081b  OPEN  2026-08-17'. Handoff line 155-156 lists push/merge as human step 1.
```

### Nothing is published: 93 commits unpushed, branch is 112 commits ahead of master, no version tag, no GitHub release, no installer anywhere public

- **Area:** release mechanics — git/GitHub  
- **Effort:** human-step · **User-facing:** yes

**Why it matters.** There is no artifact a member of the public can download, and master (the branch the README/deployment docs describe) does not contain the 0.2.0 work (mosh-cli sidecar, add-on installer, console-window fixes, licence bundle). A public release requires push, PR, merge, tag and a GitHub release with the installers attached.

**Evidence.**

```
`git branch --show-current` -> Cosmic/upbeat-golick-68081b. `git rev-list --count origin/Cosmic/upbeat-golick-68081b..HEAD` -> 93. `git rev-list --count master..HEAD` -> 112 (`git rev-list --left-right --count master...HEAD` -> `0 112`). `git branch -vv` shows `[origin/Cosmic/upbeat-golick-68081b: ahead 93]`. `git tag --list` -> only `pre-hardening-2026-07-26` (no v0.1.0 / v0.2.0). `gh release list` -> empty. The master checkout has no bundle output: `ls C:/Users/richk/CascadeProjects/moshdither-studio/src-tauri/target/release/bundle/` -> No such file or directory. docs/SESSION-HANDOFF.md:155 lists "Push the branch and open/merge the PR" as step 1 of the human checklist.
```

---

## MAJOR (41)

### Preview/export mask mismatch: effects set to 'No mask' are masked in the exported video whenever an active mask exists

- **Area:** EffectStack mask selector vs ExportPanel  
- **Effort:** small · **User-facing:** yes

**Why it matters.** Make a SAM3/manual mask, add an effect, leave it on 'No mask': preview and Save Image show the effect on the whole frame; Export Video applies it inside the mask. The export panel is the only caller that promotes the active mask to a global default.

**Evidence.**

```
EffectStack/index.tsx:210-225 offers <option value="">No mask</option> (maskId null); effectConverter.ts:1110 resolveMaskId(null) -> null so mask_b64 is null. Preview passes the GLOBAL mask as null at every site: PreviewViewport.tsx:1058 and :1115 `applyEffectStack(activeStack, null, ...)`, Toolbar.tsx:155; Save Image likewise `saveImage(activeStack, null)` (Toolbar.tsx handleSaveImage). ExportPanel/index.tsx handleExport passes `maskB64: state.activeMask ?? null` (~:269) and the FFglitch path again (~:378). Rust export commands.rs:934-940 `let active_mask = per_effect_mask.as_ref().or(global_mask.as_ref());` -- the Rust preview has the identical fallback (commands.rs:440-446) but is handed null.
```

### 'Bind Audio' on every parameter stores a binding that nothing applies -- dead in preview AND export; its live readout always reads 0

- **Area:** EffectStack/ParameterPanel AudioBindingControl  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** The control says 'Audio Bound' with source/range/attack/decay editors on every effect parameter, and the parameter never moves -- not while previewing, not in the export. Only the dedicated audio_reactive.* effects actually respond to audio (via the bake), which is a different mechanism.

**Evidence.**

```
ParameterPanel.tsx:368-375 renders AudioBindingControl for each param; toggle at :531-548 writes an AudioBinding. src/hooks/useAudioEngine.ts:60-69 registers each binding as a mapper channel named `${stackId}.${paramId}`; results go ONLY to setAudioMappedValues (useAudioEngine.ts:132-137 and 172-177). `grep -n updateStackParams src/hooks/useAudioEngine.ts src/engine/audio/*.ts` -> empty. Readers of audioMappedValues: ParameterPanel.tsx:164/375 (display) and PreviewViewport.tsx:900-903 (only .centroid/.rms/.energy/.flux global uniforms); stackToRenderPasses at PreviewViewport.tsx:888 reads unmodified store params. Export: exportVideo options (src/lib/tauri.ts:406-428) carry no bindings; Rust inject_params (src-tauri/src/audio/mod.rs:85-115) only adds `_audio_*` keys read by audio_reactive.* effects. Readout bug: ParameterPanel.tsx:530 `currentValue[binding.source]` (e.g. 'bass') but channels are keyed `${stackId}.${paramId}` (AudioParameterMapper.ts:34-39,91; useAudioEngine.ts:65), so `live` is undefined -> 0.000.
```

### Keyframes animate the preview only; the exported video bakes the playhead's value

- **Area:** EffectStack/ParameterPanel keyframe button, Timeline diamonds, AudioPanel 'Analyze Beats' -> export  
- **Effort:** large · **User-facing:** yes

**Why it matters.** A user sets two keyframes on Pixelate, scrubs and sees the block size animate, exports, and gets a static value (whatever the playhead held at export time). The feature is visibly complete in the UI and silently absent from the file.

**Evidence.**

```
src/hooks/useKeyframePlayback.ts:9-28 rewrites store params via updateStackParamsSilent at currentTime (preview mechanism). src/components/ExportPanel/index.tsx:225-229 builds the payload with stackToRustPayload(activeEffects, state.activeMask, state.sam3Masks) -- no time, no keyframes; useBatchQueue.ts:83 same. src/utils/effectConverter.ts:1123-1143: payload is {effect_id, params, mask_b64, mask_mode} only. src-tauri/src/commands.rs:324-330 `pub struct EffectCall` has no keyframe field; the export loop (commands.rs:968-995) injects only `time` and `_audio_*` per frame, params are one static map per effect. `grep -rn keyframe src/lib/tauri.ts` -> no hits; ExportPanel and useBatchQueue are absent from the list of files referencing `keyframes`. The keyframe UI is prominent: ParameterPanel.tsx:310-318 (diamond on every numeric param), Timeline/index.tsx:337-370 (diamonds on the transport strip), AudioPanel/index.tsx:92-102 ('keyframes applied').
```

### No bitstream-datamosh mode exposes a single parameter; params are hardcoded {} at every call site

- **Area:** ExportPanel FFglitch section / Toolbar / preview_ffglitch  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** MoshPro's usability is 'pick a mode, turn its knobs'. Here 25 modes are pickable and every one runs at fixed defaults; the app's signature feature has no controls.

**Evidence.**

```
ExportPanel/index.tsx:391 `applyFfglitch(moshInput, ffglitchMode, {}, finalPath)`; Toolbar.tsx:369 `{}`; src-tauri/src/commands.rs:2672-2677 preview passes `serde_json::Value::Null`. The backend reads them: packages/python-backend/mosh_cli.py:181-206 (zoom, delay, feedback, somePercentage, multiple, tailLength, threshold, origGravity, frameCount, nFrames, movementThreshold, randomness, magnitude), :252-256 (count, frame, kill, keepAudio, keepFrame), :287-299 (start, end, p, startFrame, endFrame, keepFirst, reverse, mid), :314-334 (gop, fluidity, direction, chunkSize, positionFrame, repeatCount). combine/motion_transfer need motionUrl/combineVideos (:322, :337) and are already omitted from the dropdown (ExportPanel/index.tsx:78-80).
```

### 'Add to Queue' batch exports silently drop in/out trim, watermark, audio bake, global mask and processing scale, and are always muted

- **Area:** ExportPanel batch queue / useBatchQueue  
- **Effort:** small · **User-facing:** yes

**Why it matters.** The queue looks like 'the same export, several formats'. A user who trimmed, watermarked, or relies on audio-reactive effects gets full-length, unwatermarked, static, silent files with no warning.

**Evidence.**

```
src/hooks/useBatchQueue.ts:85-93 calls exportVideo with only {maskB64:null, codec, fps, width, height, format, quality}. Compare ExportPanel/index.tsx:268-285 which also sends audioBakeJson, watermark, trimStart, trimEnd, includeAudio, processingScale, maskB64. src/lib/tauri.ts:455 `includeAudio: options.includeAudio ?? null`; src-tauri/src/commands.rs:1053 `let effective_include_audio = include_audio.unwrap_or(false);` -> queued jobs never carry the source audio. Add-to-Queue button ExportPanel/index.tsx:~906-918 copies format/codec/resolution/fps/quality only.
```

### Video mask tracking ('Run Video Predictor') produces per-frame masks the export never sees

- **Area:** MaskPanel / FrameTimeline -> export  
- **Effort:** large · **User-facing:** yes

**Why it matters.** The preview shows a mask following the subject through the clip; the exported video uses one frozen mask for every frame.

**Evidence.**

```
FrameTimeline.tsx:59-70 stores sam3FrameMasks. Readers: PreviewViewport.tsx:836, 1056, 1113, 1445-1448 and useSam3IdleShutdown.ts only; ExportPanel has no reference (grep). Export sends the single `state.activeMask` (ExportPanel/index.tsx:~225-229, :269). Extraction is from the 1280px proxy at a hardcoded 10 fps (FrameTimeline.tsx:35, 83-86) and the preview indexes masks by Math.floor(currentTime*10) (PreviewViewport.tsx:1448).
```

### SAM3: no automated test spawns the bridge or speaks its protocol; the add-on install path is tested only at the download_part level, and the assets it downloads do not exist yet

- **Area:** SAM3 / add-on coverage  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** A gate cannot run a 3 GB model, and that part is acceptable. What is missing is the cheap part: an #[ignore]-style handshake test that runs when the sidecar exists, and a dry-run of install() against a local manifest. Today the first person to exercise the download button against the real GitHub release will be a user -- and the add-on is the feature that separates this from MoshPro.

**Evidence.**

```
src-tauri/src/sam3_engine.rs: 3 #[test]s, all venv discovery (lines 1001, 1011, 1026: finds_a_venv_directly_under_the_project_root, ..._git_worktree_layout, returns_none_when_no_venv_exists); grep auth_ok in tests -> none. src/components/__tests__/Sam3Setup.test.tsx:39 vi.mock("../../lib/tauri"). tests/e2e/sam3-mask-flow.spec.ts uses tauriMockWithMediaScript (1x1 PNG masks). src-tauri/src/sam3_addon.rs tests 551-610 exercise download_part against a local TcpListener with tamper rejection (good) and 612-625 hash refusal; no test of install() end-to-end (manifest fetch -> parts -> join_and_verify -> engine locates it). SESSION-HANDOFF: 'Still untested, honestly: ... SAM3 segmentation quality (the add-on is not published yet)'; 'Until it is uploaded, the in-app installer reports that the asset list is unreachable'. src-tauri/bin/sam3-bridge-x86_64-pc-windows-msvc.exe 3,061,917,014 bytes dated 2026-09-05.
```

### When the add-on download fails, the end user is shown 'build the sidecar locally with npm run build:sam3-sidecar' verbatim

- **Area:** SAM3 add-on UX  
- **Effort:** small · **User-facing:** yes

**Why it matters.** Today (finding 1) every user who clicks Install sees a developer command line -- exactly the class the owner ruled out for non-technical users. Even after upload, any network failure surfaces the same text.

**Evidence.**

```
src-tauri/src/sam3_addon.rs:324-328: AppError::Generic(format!("Could not reach the SAM3 asset list at {url}: {e}\n\nIf this release has not published the add-on yet, build the sidecar locally with `npm run build:sam3-sidecar` instead.")). src/components/Sam3Setup.tsx:81-84 setError(msg) / setStatusMessage(`SAM3 setup failed: ${msg}`, "error"); Sam3Setup.tsx:175-177 renders {error} into the panel. Handoff line 175-178 records this behaviour as intended.
```

### Add-on download has no disk-space check, no resume, no cancel, no HTTP timeout, leaves up to ~6 GB of staging on failure, and 'about 6 GB' understates the real footprint (~10 GB peak)

- **Area:** SAM3 add-on download  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** On a laptop with 15 GB free the install can die two-thirds through with an `os error 112` string, leave gigabytes of `.part` files in ~/.moshdither/sam3/staging, and restart from byte zero next time; a stalled connection hangs the panel forever with no way out but closing the app.

**Evidence.**

```
No free-space probe: `grep -rn "available_space|free_space|sysinfo::Disk|Disks::" src-tauri/src` -> nothing. No resume: sam3_addon.rs:215 `std::fs::File::create(dest)` with a plain GET (no Range) at 202-206. No timeout: client built at 312-315 with only `.user_agent()`; `grep -n timeout src-tauri/src/sam3_addon.rs` -> nothing. No cancel: Sam3Setup.tsx:147-154 only disables the button while `installing`. Staging cleanup only on success: `remove_dir_all(&staging)` at 449, every error path returns earlier via `?`; a transfer error at 222 does not remove the partial part file (only a hash mismatch does, 239). Peak space: `join_and_verify` writes a second full copy (260-267) before the rename (404, 443). Measured sizes: sidecar 3,061,917,014 B (src-tauri/bin listing) + sam3.pt 3,450,062,241 B (~/.moshdither/models/sam3) = 6.5 GB, plus 4.8 GB %TEMP% unpack while running. Panel text Sam3Setup.tsx:105-107: "a one-time download of about 6 GB". A full disk surfaces as a raw io error string via AppError::Io -> `e.to_string()` at 303-305.
```

### The ~6 GB SAM3 add-on download cannot be cancelled or resumed, checks no free disk space, and keeps running invisibly if the panel is closed

- **Area:** SAM3 add-on install  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** A non-technical user on a laptop starts a 6 GB download, closes the panel, and has no way to see or stop it; if it fails at 5.9 GB it starts over from zero next time. This is the first-run experience for the AI feature.

**Evidence.**

```
src-tauri/src/sam3_addon.rs:303-421 `install()` has no cancellation token or flag; :218 `std::fs::File::create(dest)` truncates each part on every attempt (no Range/resume); parts are only deleted on success (:404-406, :450-452) and the staging dir only at :418; `grep -n -i 'available_space\|free_space\|disk' src-tauri/src/sam3_addon.rs src/components/Sam3Setup.tsx` finds no free-space check; a full disk surfaces as the raw io error via :298 `.map_err(|e| e.to_string())`. src/components/Sam3Setup.tsx:143-190 renders one button ("Download and install SAM3") and a progress bar -- no cancel control; :53-68 the progress listener is removed on unmount and `installing` is component-local state, so navigating away hides a download that continues.
```

### A SAM3 bridge that dies at startup is reported as a 300-second timeout plus an environment-variable instruction

- **Area:** SAM3 error reporting  
- **Effort:** small · **User-facing:** yes

**Why it matters.** Any startup crash of the 3 GB sidecar (missing runtime DLL, corrupt unpack, out of memory) tells the user it 'timed out after 300s' when it died instantly, and the suggested remedy is to set an env var. The real cause is in a log file the UI never points them to from this path.

**Evidence.**

```
sam3_engine.rs:523-528 maps every `recv_timeout` error, including `RecvTimeoutError::Disconnected` (the reader thread exits on EOF at 479-483 and drops `tx`), to: "SAM3 auth handshake timed out after {:?}: {}. A bundled sidecar unpacks several GB on first launch; set MOSHDITHER_SAM3_AUTH_TIMEOUT_SECS to raise this budget." The bridge's actual stderr goes only to the log (sam3_engine.rs:453). commands.rs:1240 wraps it as "Failed to start SAM3 engine: ...", MaskPanel.tsx:92 shows `SAM3 start failed: ...`.
```

### Every SAM3 start unpacks 4.8 GB into %TEMP%, and every crash or kill leaves it there (19+ GB leaked on the dev machine)

- **Area:** SAM3 sidecar packaging / temp hygiene  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** A user with SAM3 installed loses 4.8 GB of free disk per app crash and pays a 30-45 s unexplained wait on every session; on the owner's near-full machine this is exactly the kind of pressure that feeds the 0xc0000374/commit-limit theory. `--onedir` (the add-on is already assembled from parts, so a directory install is natural) removes both the per-launch unpack and the leak.

**Evidence.**

```
scripts/build-sam3-sidecar.py:204 `"--onefile"`. sam3_engine.rs:343-349 comment measures cold starts at 29.7 s, 33.8 s, 44.3 s for the unpack. Measured on this machine: `ls $TEMP | grep _MEI` -> nine `_MEI*` directories; `du -sh` (still running when cut off) -> _MEI309922 4.8G, _MEI378962 4.8G, _MEI547682 4.8G, _MEI557922 4.8G, _MEI0000e5fc2 2.3M. Leak paths in code: `cleanup_stale_sam3_bridge` reaps an orphan with `process.kill()` (sam3_engine.rs:134), `Drop for Sam3Engine` calls `kill_child` with no graceful shutdown (963-967), and the app's own unreproduced export crash orphans a running bridge. During the wait the UI shows only "Starting SAM3 engine..." (MaskPanel.tsx:67) / "Starting..." (346).
```

### Two 'Export FFglitch' controls behave differently: File menu moshes the untouched source with a hardcoded 'classic' mode

- **Area:** Toolbar File menu vs ExportPanel FFglitch section  
- **Effort:** small · **User-facing:** yes

**Why it matters.** A user who builds a stack, picks 'Glide' in the Export panel, then uses File > Export FFglitch gets a Classic mosh of the raw clip with every effect discarded and no message saying so.

**Evidence.**

```
src/components/Toolbar.tsx:369 `const outPath = await applyFfglitch(path, "classic", {});` -- ignores store `ffglitchMode`, the effect stack, masks, LUTs and in/out; menu item at Toolbar.tsx:~520-535 labelled 'Export FFglitch'. ExportPanel/index.tsx:347-406 renders the effect stack to an intermediate first, then applies `ffglitchMode` (line 391). Commit e69b58d 'fix(export): datamosh the rendered effects, not the untouched source' fixed only the panel path (git log -- src/components/ExportPanel/index.tsx).
```

### File > Export Video does nothing when the Export panel is closed, then fires an unprompted export when the panel is next opened

- **Area:** Toolbar handleExport / ExportPanel exportTriggerId  
- **Effort:** small · **User-facing:** yes

**Why it matters.** Close the Export tab, use File > Export Video: status claims an export started, nothing happens. Later dock Export from the rail: a Save dialog appears on its own because the stale trigger is > 0 on mount.

**Evidence.**

```
Toolbar.tsx:339-348 `state.triggerExport(); setStatusMessage("Export started from Export panel...")`. Sole consumer: ExportPanel/index.tsx:166-168 `exportTriggerId`, `lastTriggerId = useRef(0)`, effect at :306-311 `if (exportTriggerId > 0 && exportTriggerId !== lastTriggerId.current) { ... void handleExportRef.current(); }`. The panel is lazy-mounted only when docked (DockLayout.tsx:100-143). Panels are closable: defaultLayout.ts:5 `tabEnableClose: true`; Edit > Hide All (Toolbar.tsx:~650) removes every registry panel.
```

### Tracks panel creates layers with opacity/blend mode that nothing renders or exports; onboarding advertises that feature

- **Area:** TrackPanel / panelRegistry / OnboardingModal  
- **Effort:** small · **User-facing:** yes

**Why it matters.** A first-run user is told to use blend modes and opacity, finds a Tracks panel with exactly those controls, and nothing changes on screen or in the file.

**Evidence.**

```
`grep -rn "\.tracks\b\|activeTrackId" src` (excluding TrackPanel/tests) -> only src/store/index.ts:296,618,1240-1297 (mutators). No reader in PreviewViewport, ExportPanel, effectConverter or Rust. panelRegistry.ts:88-96 puts it on the rail; Toolbar.tsx:600-640 lists it under Edit > Panels. OnboardingModal.tsx:75 'Use blend modes and opacity for fine control' -- StackEntry (store/index.ts:36-45) has no opacity/blendMode; only Track does (TrackPanel.tsx:106-134). SESSION-HANDOFF.md acknowledges: 'Tracks panel creates entries nothing renders or exports'.
```

### Uninstalling leaves ~6.5 GB in %USERPROFILE%\.moshdither and the app offers no way to remove SAM3

- **Area:** Uninstall / disk footprint  
- **Effort:** small · **User-facing:** yes

**Why it matters.** A non-technical user who tries SAM3 and uninstalls keeps 6.5 GB of hidden dot-folder data forever with no UI telling them it exists; the 'try it and remove it' path MoshPro users expect does not exist.

**Evidence.**

```
Add-on lives outside every directory the installer knows: sam3_engine.rs:169-174 (`~/.moshdither/sam3`), sam3_addon.rs:134-144 (`~/.moshdither/models/sam3/sam3.pt`). Only two commands exist: sam3_addon.rs:158 `sam3_addon_status`, 303 `sam3_addon_install`; `grep -n remove src-tauri/src/sam3_addon.rs` shows only part/staging cleanup. Sam3Setup.tsx (read in full, 182 lines) has no remove control. tauri.conf.json has no `bundle.windows.nsis` block, so the stock uninstaller only handles $INSTDIR and `$APPDATA/com.moshdither.studio` (the scopes the app itself declares in capabilities/default.json:10-13). docs/SESSION-HANDOFF.md:26-27 already records that the uninstaller misses a stale sidecar.
```

### "Save Project" does not save the whole session: audio file, in/out points, datamosh mode, export settings, guides, watermark and SAM3 masks are dropped

- **Area:** data-safety / project file  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** A user who trims a clip, picks a mosh mode and export settings, saves, and reopens gets a different project back and has to redo the parts that are not effects; the 0xc0000374 export crash recovery path loses the same fields.

**Evidence.**

```
src/hooks/useProject.ts:7-17 `interface ProjectFile { version; createdAt; effectStack; keyframes; audioBindings; mediaFilePath; activeMask }`. Store fields with no counterpart: src/store/index.ts:169 `audioFilePath: string | null;`, :110-111 `inPoint`/`outPoint`, :146 `ffglitchMode`, :150 `exportFormat`, :154 `exportQuality`, :199 `viewportGuides`, :226 `sam3Masks`, :303 `watermark`. audioBindings are saved (useProject.ts:52) but the audio path they bind to is not, so a reopened project has reactive bindings with no audio. The autosave `ProjectSession` (useProjectSession.ts:10-19) includes audioFilePath but likewise omits in/out, activeMask, sam3Masks, ffglitchMode and export settings.
```

### Open Project wipes the current stack before validating the file; a malformed or old .moshdither says "Project loaded" and throws asynchronously, and there is no unsaved-changes prompt on Open

- **Area:** data-safety / project open  
- **Effort:** small · **User-facing:** yes

**Why it matters.** A non-technical user who opens an older or damaged project, or simply opens any project while work is in progress, loses the current stack with a success message and no undo; the autosave copy is gone 5 s later.

**Evidence.**

```
src/hooks/useProject.ts:77-81 `const project = JSON.parse(raw) as ProjectFile; if (project.version !== PROJECT_VERSION) { setStatusMessage(`Warning: ...`) }` -- no shape check, version mismatch only warns. Line 87 `clearStack();` runs synchronously, then lines 88-89 `setTimeout(() => { for (const entry of project.effectStack) {` with no try/catch inside the timer, then line 137 `setStatusMessage(`Project loaded: ${path}`)`. A file containing `{}` or `{"version":1}` therefore clears the user's stack, reports success, and throws "project.effectStack is not iterable" a tick later. useProjectSession.ts:96-121 then overwrites the localStorage autosave with the emptied stack within 5 s (`if (filePath || ... )` -- filePath is still set). Callers invoke it with no confirm: src/components/Toolbar.tsx:573 `onClick={() => { void openProject(); ...`, src/hooks/useKeyboardShortcuts.ts:80 and :221 `openProject()`; grep for unsaved/isDirty/confirm( around these sites returns nothing. src/hooks/useProject.edgeCases.test.ts:104-118 only asserts `JSON.parse` semantics (`expect(parsed.effectStack).toBeUndefined()`), not the hook's behaviour.
```

### FFglitch mode knobs are unreachable: params hardcoded {} at both call sites; combine/motion_transfer have no UI

- **Area:** datamosh feature parity  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** MoshPro's core UX is per-mode parameters; here 25 modes run only at backend defaults (e.g. mosh_cli.py:287-294 params.get("p", 5), params.get("end", 10)).

**Evidence.**

```
src/components/ExportPanel/index.tsx:404 `applyFfglitch(moshInput, ffglitchMode, {}, finalPath)`; src/components/Toolbar.tsx:374 `applyFfglitch(path, "classic", {})`. Rust validates motionUrl/combineVideos (src-tauri/src/commands.rs:2499-2520) but nothing supplies them; ExportPanel/index.tsx:82-84 comment: 'combine, motion_transfer -- these need a second video / a motion source, and there is no UI to supply one'. Handoff line 107-109 lists this as open.
```

### Datamoshing (mosh-cli / ffgac / ffedit, the product's signature feature) has zero automated execution coverage -- every automated check stops at 'the sidecar file exists'

- **Area:** datamosh pipeline coverage  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** The 25 offered modes and the 7 removed ones are known-good/known-bad only from manual runs on one clip. A change to mosh_cli.py, basic_modes.py, the PyInstaller spec, or the sidecar's CREATE_NO_WINDOW handling passes every gate. For a datamosh tool, that is the feature most likely to be what a user tries first.

**Evidence.**

```
src-tauri/src/commands.rs: locate_mosh_cli at 2264, preview_ffglitch 2598, apply_ffglitch 2683 -- all outside the four #[cfg(test)] regions (lines 1399, 2855, 2956, 3074); the only ffglitch tests (3086-3138) validate motion_transfer/combine parameter paths. grep for mosh-cli|ffgac|ffedit|apply_ffglitch in test fns -> only functional_tests.rs existence checks (lines 434-459, 603-645). tests/e2e mocks: 'apply_ffglitch: "C:/tmp/moshdither-export/test_ffglitch.mp4"'. packages/python-backend has no tests (find test_*.py -> only sam3_env_cpu site-packages). SESSION-HANDOFF: '7 of 8 tomato modes produce 0 bytes on real footage ... content-dependent' -- found by hand; 'verified end to end against a real clip with no system Python' -- by hand.
```

### app-smoke gate's PANIC detection is dead code: $before and $log are never assigned, so only 'process still alive after 20 s' is checked

- **Area:** evals/regression.ps1 gate 10  
- **Effort:** small · **User-facing:** no

**Why it matters.** A startup panic that Tauri logs but that does not terminate the process (e.g. in a spawned thread, a plugin, or a caught unwind in setup) passes gate 10 while the comment above it claims it catches exactly that class. The one historic case (updater PluginInitialization panic) did exit the process, so the alive-check would have caught it -- but the gate is weaker than its own comments say.

**Evidence.**

```
evals/regression.ps1:80-81 and 87-88: 'Get-ChildItem (Join-Path $env:USERPROFILE ".moshdither" "logs") -Filter *.log ... | Sort-Object LastWriteTime -Descending | Select-Object -First 1' -- piped to output, no '$before =' / '$log ='. Line 89: 'if ($log -and (-not $before -or $log.Name -ne $before.Name)) {' -- $log is $null so the branch (Select-String -Pattern 'PANIC', lines 90-96) never executes. git show 02d5a01:evals/regression.ps1 lines 71-80 has the identical shape: the assignment never existed; 9666c04 only changed throw -> $global:LASTEXITCODE.
```

### app-smoke launches whatever moshdither-studio.exe already exists; the gate never rebuilds it, and the release exe embeds the frontend at compile time -- both logged runs smoke-tested a build two commits stale

- **Area:** evals/regression.ps1 gate 10 / staleness  
- **Effort:** small · **User-facing:** no

**Why it matters.** Gate 10 exists because 'every other gate can pass while the shipped app cannot start' (its own comment). As written it cannot detect a startup regression introduced by the tree being gated unless someone separately ran 'tauri build' first. Together with the dead PANIC check, gate 10 currently proves only that SOME earlier build survives 20 s.

**Evidence.**

```
evals/regression.ps1:65-67: 'if (-not (Test-Path $exe)) { & cargo build --release ... }' -- build only when missing. Gate 8 (line 47) is 'cargo build --release --bin mosh-verify' only; both logs show '[8/10] mosh-verify release binary'. src-tauri/tauri.conf.json build.frontendDist = "../dist" (embedded into the binary by tauri build). scratchpad build_v9.log mtime 17:08:35 and build_v10.log 17:56:40 (post-commit); 'ls src-tauri/target/release/moshdither-studio.exe' -> 2026-09-09 17:56:40. The 17:25 and 17:39 gate runs therefore launched the 17:08 exe, which predates commits 3b9680e (17:35) and 274502f (17:50). Contrast: tests/e2e/real-backend.spec.ts:84+ refuses to run mosh-verify older than the newest .rs -- gate 10 has no such check.
```

### Cancel is ignored during decode and effects (most of an export); the UI reports 'Export cancelled' while the job keeps running and holds the export slot

- **Area:** export / cancellation  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** The effects stage is the slow one (progress 5-85). A user who cancels a 5-minute export sees it vanish, then the machine stays pegged and the next export appears stuck for minutes. Combined with the memory findings, the phantom job is still holding gigabytes.

**Evidence.**

```
src-tauri/src/commands.rs:747-1083 `export_video_blocking`: `cancel` appears only as the parameter (:766) and the pass-through to `encode_video` (:1072); the decode call :838 `decode_video_with_options(&source_path, None, decode_scale)` takes no cancel and `output_with_timeout` (ffmpeg/mod.rs:1458-1540) has no cancel; the effects loop :918-1040 never reads the flag. The only poll is ffmpeg/mod.rs:1310-1321 inside the encode write loop. src/components/ExportPanel/index.tsx:313-328 `handleCancel` calls `cancelExport()` then immediately `setExportIsRunning(false); setStatusMessage("Export cancelled")`. The running job still owns the one-permit semaphore (commands.rs:711-717 `export_semaphore.acquire_owned().await` before `cancel.store(false)`), so a second Export click waits silently at "Export started...".
```

### Export peak memory is 2-3x the budget the code checks; a 4 GiB budget means ~8 GiB at decode and ~12 GiB during masked effects

- **Area:** export / memory  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** The budget is presented as a safety limit but the pipeline exceeds it by design; on the owner's machine that is the commit-limit hypothesis, on a user's laptop it is an OOM abort with no log line. Fixing the still-image finding does not fix this.

**Evidence.**

```
src-tauri/src/ffmpeg/mod.rs:16-21 own comment: "`cmd.output()` buffers all of FFmpeg's stdout in memory before we chunk it into frames, so peak memory is ~2x the raw RGBA size"; :1489-1497 `output_with_timeout` does `reader.read_to_end(&mut data)` into one Vec; :570-580 `chunk.to_vec()` copies each frame while `raw` is still alive. src-tauri/src/commands.rs:938-942 `previous_frames = Some(segment.frames.clone())` for any effect with an active mask that does not self-mask (+1x); :966-981 non-temporal effects `par_iter().map(...).collect()` into a new Vec while `segment.frames` still exists (+1x, repeated per effect). The budget check (ffmpeg/mod.rs:486-501) only bounds the decoded Vec. Owner's machine sits at 107/110 GB commit (docs/SESSION-HANDOFF.md:112-117); on a 16 GB laptop 12 GiB is the commit limit.
```

### Last full gate on record FAILED (e2e); the commit that followed was verified by a partial Playwright run, yet the handoff files it under 'all gated 10/10'

- **Area:** gate evidence / release record  
- **Effort:** small · **User-facing:** no

**Why it matters.** The failure was a stale test locator, not an app bug, but the release claim rests on a full-gate run that does not exist in any log. The gate is the ONLY signal on this repo (CI never runs), so a '10/10' that was actually 'one spec re-run' is the exact kind of record the handoff warns against trusting.

**Evidence.**

```
gate_strip.log (started 2026-09-09 17:39:39): '[9/10] Playwright E2E suite FAIL 392.4s' ... '1) [chromium] › tests\e2e\timeline.spec.ts:71:1 › play/pause button toggles between Play and Pause' / 'Locator: locator('.flexlayout__tab').filter(...)' / 'Error: element(s) not found'. gate_timeline.log (17:25:59) is the only 10/10 run and predates the strip change. tests/e2e/timeline.spec.ts mtime 17:48:24 (git show 274502f: locator changed to page.getByTestId("transport-strip")). test-results/.last-run.json = {"status":"passed","failedTests":[]} written 17:49:37 -- 73 s after the spec edit; a full suite takes 392-408 s per both logs. Commit 274502f 17:50:01 'feat(timeline): make it a transport strip'. docs/SESSION-HANDOFF.md places '**Timeline is a transport strip now**' in the table headed '**Fixed (all gated 10/10, committed):**'.
```

### E2E gate failed in 4 of the day's 10-gate runs on 4 different tests, and one Rust-test run died from rust_oom inside rustc itself -- the only real signal is being drowned by the environment

- **Area:** gate reliability / machine commit limit  
- **Effort:** human-step · **User-facing:** no

**Why it matters.** When the gate reports FAIL for a different reason every hour, a real regression hides inside the noise and the operator learns to re-run until green. The rustc OOM is also direct evidence for the commit-limit theory behind the unreproduced 0xc0000374 export crash. Part fix is code (see other findings), part is the pagefile -- a human step.

**Evidence.**

```
scratchpad logs: gate_ux1.log (15:33:09) 'Failed: vitest, e2e, app-smoke' (vitest: 'FAIL tests/scripts/sam3-build.spec.ts > SAM3 build infrastructure > generates a release Tauri overlay'; e2e '1 failed 76 passed (7.4m)'); gate_ux3.log (16:06) 'Failed: cargo-test, e2e, app-smoke' -- cargo-test exit 101 with 'std::alloc::rust_oom' under 'rustc_monomorphize::partitioning::collect_and_partition_mono_items' (the COMPILER ran out of commit), e2e '1) webgl-context-lifecycle.spec.ts:44'; gate_ux5.log (16:40) e2e '1 failed: app-launch.spec.ts:43 no console errors on launch'; gate_strip.log (17:39) e2e timeline.spec.ts:71. playwright.config.ts: 'retries: process.env.CI ? 2 : 0' (no local retries, so these are honest). vite.config.ts test block comment: 'a gate you learn to re-run is a gate you have stopped trusting'. SESSION-HANDOFF.md: 'A "gate failure" with a burst of unrelated errors is usually resource contention ... Re-run it alone before believing it.'
```

### GPL corresponding-source tarballs are not staged anywhere for the release

- **Area:** licensing  
- **Effort:** human-step · **User-facing:** no

**Why it matters.** The installer redistributes GPL FFmpeg 8.0 (ffmpeg/ffprobe) and GPLv2+ FFglitch 0.10.2 (ffgac/ffedit); shipping without the offer of source is the doc's own stated obligation.

**Evidence.**

```
find . -maxdepth 3 -name ffmpeg-8.0.tar.xz -o -name ffglitch-0.10.2.tar.xz -> no results; build-archive/ holds only three 0.1.0 installer pairs (Aug 23) and sam3-addon/. THIRD-PARTY-NOTICES.md 'Release checklist' steps 1-2: download both tarballs and attach them to the same release as the installer; handoff line 160-163 calls it 'a real obligation, not a nicety'.
```

### Third-party notices omit the vendored DatamoshLib authors; the Akash Bora 'Original' modes have no recorded licence at all

- **Area:** licensing  
- **Effort:** medium · **User-facing:** no

**Why it matters.** MIT requires the copyright notice to accompany copies; mosh-cli.exe is a PyInstaller bundle of this code shipped in the installer. Code of unknown licence is being redistributed as MIT.

**Evidence.**

```
THIRD-PARTY-NOTICES.md 'Datamosh sidecar' section names only 'vendored DatamoshLib and pymosh', NumPy (BSD) and CPython (PSF), while the file's preamble says 'This file names every one of them'. On disk: packages/python-backend/DatamoshLib/Tomato/LICENSE.txt = 'MIT License Copyright (c) 2017 Kaspar RAVEL'; packages/python-backend/pymosh/LICENSE.txt = MIT 'Copyright (c) 2011 - 2014 Joe Friedl'; packages/python-backend/DatamoshLib/Original/{classic,classic_new,pymodes,repeat}.py each begin '#Author: Akash Bora' with no LICENSE/COPYING under DatamoshLib/Original or FFG_effects (find -iname '*licen*' -> only Tomato/LICENSE.txt); packages/python-backend/README.md:47-49 asserts 'License: Same as root project (MIT)' for the whole directory. licenses/ (bundled per tauri.conf.json resources) contains no MIT notice for Ravel or Friedl.
```

### Installers are unsigned; Windows SmartScreen will warn every first-run user

- **Area:** packaging — code signing  
- **Effort:** human-step · **User-facing:** yes

**Why it matters.** For a non-technical audience an unsigned installer produces the 'Windows protected your PC' wall on every install until reputation accrues; a meaningful share of MoshPro-style users will abandon at that dialog. Requires purchasing an Authenticode certificate — a human step.

**Evidence.**

```
src-tauri/tauri.conf.json bundle.windows: `"certificateThumbprint": null, "signCommand": null`. README.md:97-112 "### Code signing — not currently configured ... Releases are **unsigned**, so Windows SmartScreen will warn on first run". docs/SESSION-HANDOFF.md:164-165 and :192-193 say the same. .github/workflows/release.yml:139-156 would generate tauri.windows.conf.json from a WINDOWS_CERTIFICATE secret, but Actions never runs on this repo.
```

### Installer contents depend on gitignored on-disk state: 676 files of the gitignored SAM-licensed `sam3_repo/` source tree and 161 `__pycache__` .pyc files are bundled, and a fresh clone would produce a different installer

- **Area:** packaging — resources  
- **Effort:** small · **User-facing:** no

**Why it matters.** Roughly 74 MB of unneeded, separately-licensed third-party source ships in every installer purely because it happened to be on the build machine; the same command on a fresh clone yields a materially different artifact, so 'what shipped' is not reproducible from the repo. .pyc files compiled for the owner's CPython 3.13 are dead weight.

**Evidence.**

```
src-tauri/tauri.conf.json resources: `"../packages/python-backend": "packages/python-backend"` (whole directory). `git status --ignored --short packages/python-backend` -> `!! packages/python-backend/sam3_repo/` plus six `__pycache__/` dirs; `git ls-files packages/python-backend | wc -l` -> 32; `du -sh packages/python-backend` -> 75M, of which `packages/python-backend/sam3_repo` -> 74M (its LICENSE begins "SAM License / Last Updated: November 19, 2025"). 7z listing of the 0.2.0 NSIS: `grep -c sam3_repo` -> 676, `grep -c __pycache__` -> 161 (e.g. `packages\python-backend\sam3_repo\LICENSE`, `...\DatamoshLib\FFG_effects\__pycache__\basic_modes.cpython-313.pyc`); total 814 files, 416,573,781 B uncompressed. The frozen sidecar does not need it: src-tauri/sam3_bridge.py:145-149 falls back to `sys._MEIPASS` when SAM3_REPO is unset, and scripts/build-sam3-sidecar.py:215 `--add-data={sam3_pkg}`. THIRD-PARTY-NOTICES.md lists Meta's sam3 source only as part of the sidecar (lines 120-124), not as a separately bundled source tree in the installer.
```

### Tracks and Proxy Media panels ship in the default rail; Tracks creates entries nothing renders or exports

- **Area:** panels / usability  
- **Effort:** small · **User-facing:** yes

**Why it matters.** A first-time user is greeted by 10 panels, one of which is a control that does nothing -- the class of defect the 2026-08-23 audit was hunting.

**Evidence.**

```
src/store/index.ts:577-591 panelVisibility defaults: proxy: true, tracks: true (only verify hidden). src/components/DockSystem/panelRegistry.ts:80-90 registers 'Proxy Media' and 'Tracks'. Readers of `tracks` outside the store: Timeline/index.tsx, TrackPanel.tsx, ViewportGuides.tsx, panelRegistry.ts only; grep '\.tracks\b' src/lib/export* src/engine -> none. Handoff line 110-111 'Tracks panel creates entries nothing renders or exports ... removing it from the rail is a product call'; PRODUCT-REVIEW.md:147-157 recommends a default of five panels -- not applied.
```

### Not code-signed: SmartScreen warns on every install

- **Area:** release / signing  
- **Effort:** human-step · **User-facing:** yes

**Why it matters.** For a non-technical audience the first interaction with the product is a Windows warning; documented as accepted, but it is the first thing a MoshPro user will compare.

**Evidence.**

```
src-tauri/tauri.conf.json bundle.windows: "certificateThumbprint": null, "signCommand": null; README.md:97-112 'Code signing -- not currently configured ... Releases are unsigned, so Windows SmartScreen will warn on first run'. Handoff checklist item 4.
```

### Release documentation contradicts the code and itself in ways that would misdirect the person doing the release (stale signing vars, wrong build command, wrong sizes, wrong version)

- **Area:** release mechanics — docs  
- **Effort:** small · **User-facing:** no

**Why it matters.** The owner will follow these documents to cut the release. Two of them send the releaser down paths that are known to fail silently (electron-builder signing vars) or hard (with-sidecar build), and the handoff's own state section cannot be trusted line-by-line.

**Evidence.**

```
docs/deployment.md:75-92 §5 Code Signing gives `WIN_CSC_LINK`, `WIN_CSC_KEY_PASSWORD`, `CSC_LINK` — README.md:103-107 states "**Tauri v2 reads none of those** ... produced a silently unsigned installer". deployment.md:99 §6 step 5 `npm run tauri:build` = the with-sidecar path that src-tauri/src/sam3_engine.rs:164-168 says cannot produce a Windows installer; step 3 bumps only package.json and tauri.conf.json (Cargo.toml:3 also carries the version). deployment.md:40-48 sidecar table omits mosh-cli (tauri.conf.json externalBin has 5 entries). README.md:36 "FFmpeg binaries (downloaded automatically on first build)" — package.json `prebuild` runs verify-external-bins.mjs, which `fail()`s at :131-136 with "Run: npm run fetch:external"; nothing downloads automatically. README.md:63 says put LUTs in `src-tauri/resources/lut/`; tauri.conf.json bundles `../public/lut`. docs/SESSION-HANDOFF.md:45 "NSIS ~156 MB + MSI ~185 MB" vs actual 188,531,067 / 219,603,432 B; :179-181 "target/release/sam3-bridge.exe is a STALE 297 MB CPU-only build" vs `ls src-tauri/target/release/sam3-bridge.exe` -> 3,061,917,014 B Sep 5; :194-198 "updater is configured ... pubkey and endpoint are set" vs `git diff master -- src-tauri/tauri.conf.json` showing the whole `plugins.updater` block removed; :200 "Version is still `0.1.0`" vs :45 "Version is **0.2.0**".
```

### GPL corresponding-source obligation is unfulfilled: the two source tarballs named in THIRD-PARTY-NOTICES are not on disk and there is no release to attach them to

- **Area:** release mechanics — licensing  
- **Effort:** human-step · **User-facing:** no

**Why it matters.** Publishing the installer without the corresponding source (or a durable pointer the notices file relies on) is a GPLv3 §6 compliance gap from day one of a public release.

**Evidence.**

```
THIRD-PARTY-NOTICES.md:130-149 "## Release checklist (GPL corresponding source) ... 1. Download the two source tarballs named above. 2. Attach both to the same release as the installer"; sources named at :54 (ffmpeg-8.0.tar.xz) and :70 (ffglitch-0.10.2.tar.xz). `ls build-archive/*.tar* build-archive/*.zip src-tauri/bin/*.zip` -> No such file; `find . -maxdepth 3 -iname '*source*.tar*' -o -iname '*corresponding*'` -> nothing. docs/SESSION-HANDOFF.md:161-163 "GPL corresponding source ... This is a real obligation, not a nicety." The installer redistributes ffmpeg.exe/ffprobe.exe/ffgac.exe/ffedit.exe (7z listing).
```

### The 0.2.0 installers were built from a HEAD that has no complete passing gate run on record; the last recorded run failed the Playwright E2E gate

- **Area:** release mechanics — verification  
- **Effort:** small · **User-facing:** no

**Why it matters.** The local gate is the only real signal on this project (CI never runs). The exact tree inside the 0.2.0 installers (transport strip + rewritten E2E test) has never been shown to pass all 10 gates; the rewritten test may pass, but that is an assumption, not a record.

**Evidence.**

```
gate_timeline.log header `2026-09-09 17:25:59 | 10 gates` -> `ALL 10 GATES PASSED`. gate_strip.log header `2026-09-09 17:39:39 | 10 gates` -> `[9/10] Playwright E2E suite FAIL 392.4s` ... `1 GATE(S) FAILED`, failing test `tests\e2e\timeline.spec.ts:71:1 › play/pause button toggles between Play and Pause` (locator `.flexlayout__tab` ... element(s) not found); file mtime 17:47:53. `git log -3 --format='%h %ci %s'` -> 274502f 2026-09-09 17:50:01 feat(timeline): make it a transport strip; 3b9680e 17:35:00 fix(timeline): one clock. `git log -1 -- tests/e2e/timeline.spec.ts` -> 274502f, and tests/e2e/timeline.spec.ts:71-85 at HEAD now scopes to `page.getByTestId("transport-strip")`. Installers: `MoshDither Studio_0.2.0_x64_en-US.msi` mtime Sep 9 17:54, `MoshDither Studio_0.2.0_x64-setup.exe` mtime Sep 9 17:56. No gate log exists after 17:50.
```

### CHANGELOG 0.2.0 does not describe the 0.2.0 installer: 18 commits (72 files, +9703/-391) landed after the changelog cut and `[Unreleased]` is empty; same version string now labels two different builds

- **Area:** release mechanics — versioning/changelog  
- **Effort:** small · **User-facing:** yes

**Why it matters.** Release notes a user would read for 0.2.0 omit the timeline rework, datamosh mode picker, shortcut fixes and console-window fix that are in the binary; and with two different 0.2.0 installers on disk and none SHA-archived, 'which build shipped' cannot be answered later — the exact trap archive-build.mjs was written to prevent.

**Evidence.**

```
CHANGELOG.md:10-12 `## [Unreleased]` (empty) then `## [0.2.0] - 2026-09-08`; `git log -3 -- CHANGELOG.md` -> last touch fa8ff37 2026-09-08 23:32:57 "docs: make the release docs true, and ship 0.2.0". `git log --oneline fa8ff37..HEAD` -> 18 commits including 274502f feat(timeline) transport strip, 3b9680e one clock, d832861 "stop the console window for real", 0e7a043 34 bitstream modes, c84700d recorded shortcuts / mosh modes / export settings; `git diff --stat fa8ff37..HEAD | tail -1` -> 72 files changed, 9703 insertions(+), 391 deletions(-). Both `MoshDither Studio_0.2.0_x64_en-US.msi` (Sep 9 17:54) and the Sep 8 21:57 build in the same dir use the 0.2.0 name; scripts/archive-build.mjs:6-11 explains Tauri overwrites the same filename; build-archive/ holds only 0.1.0_<sha> archives dated Aug 23 — the 0.2.0 builds were never archived under a SHA.
```

### HEAD (274502f) has no passing gate run behind it; the last logged gate FAILED the e2e suite and the commit that changed the test landed afterwards

- **Area:** release process  
- **Effort:** human-step · **User-facing:** no

**Why it matters.** The local gate is the only real signal on this repo (CI never runs). The last commit before release changed the layout, the e2e spec and the dock system, and the only evidence on file for that state is a red gate.

**Evidence.**

```
gate_strip.log (scratchpad, mtime 17:47): `[9/10] Playwright E2E suite  FAIL  392.4s` -- `timeline.spec.ts:71 play/pause button toggles` could not find `.flexlayout__tab ... getByTitle(/^(Play|Pause)$/)`. `git log -3 --format='%h %cd %s' --date=iso` -> `274502f 2026-09-09 17:50:01 feat(timeline): make it a transport strip`, which per `git show --stat 274502f` changed tests/e2e/timeline.spec.ts (+10/-...). `ls -lt <scratchpad> | grep gate` shows nothing newer than gate_strip.log 17:47. docs/SESSION-HANDOFF.md:81 claims "Fixed (all gated 10/10, committed)". The spec now uses `getByTestId("transport-strip")` (tests/e2e/timeline.spec.ts:76) and `data-testid="transport-strip"` exists at src/components/Timeline/index.tsx:154, so the fix is plausible but unproven.
```

### Every user-facing flow in the E2E suite runs against a JavaScript stub of the Rust backend: real export, real file/save dialogs, real SAM3 and the add-on download are never driven from the UI by any gate

- **Area:** tests/e2e mocks  
- **Effort:** human-step · **User-facing:** yes

**Why it matters.** For a desktop app, mocking the IPC in browser E2E is normal and acceptable -- IF a human exercises open -> stack -> export -> save on the INSTALLED build before release. SESSION-HANDOFF.md 'Open' #1 says exactly that has not happened ('neither has been exercised by a human in the installed app'). Until it does, nothing automated has ever watched a user click Export and get a file.

**Evidence.**

```
tests/e2e/mocks/tauri-mock.ts and tauri-mock-with-media.ts replace window.__TAURI_INTERNALS__.invoke: 'export_video: "C:/tmp/moshdither-export/test_export.mp4"', 'apply_ffglitch: "C:/tmp/.../test_ffglitch.mp4"', 'save_media: ...', 'if (command === "plugin:dialog|open") return Promise.resolve("C:/tmp/moshdither/test_clip.mp4")', 'plugin:dialog|save' -> canned path or null, 'sam3_auto_mask: { count: 2, masks: [tinyPng, tinyPng] }'. 18 of 23 specs import a mock (grep -ln tauriMock). tests/e2e/export-flow.spec.ts asserts only '[data-testid="status-message"] hasText /Exported:/' after the mock resolves. The 5 unmocked specs: real-backend.spec.ts + render-presets-luts.spec.ts drive the mosh-verify CLI (its header: 'Why mosh-verify instead of tauri-driver? ... flaky'), run-parity (see separate finding), stress-test, webgl-context-lifecycle. No spec uses tauri-driver/WebDriver against the real app. src/components/__tests__/ExportPanel.test.tsx lines 9-41 mock @tauri-apps/api/core, plugin-dialog, api/event, ../../lib/tauri.
```

### run-parity.spec.ts is a tautology: in the last gate run it compared two 1x1 placeholder PNGs, so WebGL-preview-vs-CPU-render parity has no real automated check

- **Area:** tests/e2e/run-parity.spec.ts  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** The product promise is that what the user sees in the preview is what export writes ('WebGL while interacting, exact CPU frame 450 ms after idle'). The one test named for that property cannot fail. Contrast live-preview-param-update.spec.ts, which does capture real pixels (46 KB / 93 KB PNGs at 17:43).

**Evidence.**

```
tests/e2e/run-parity.spec.ts: imports no Tauri mock (grep -L tauriMock lists it), so no media is ever loaded; lines 33-55: 'if (await canvas.first().isVisible()) {...} else { fs.writeFileSync(webglPath, Buffer.from("iVBORw0KGgoAAAANSUhEUgAAAAEAAAAB...", "base64")) }' and the same for cpuPath; catch block lines 70-76 rethrows only 'if (e.message && e.message.includes("FAIL"))' -- a missing python passes. ls -la tests/e2e/output: 'webgl.png 70 bytes 17:45:18', 'cpu.png 70 bytes 17:45:18' (written during the gate_strip run) -- i.e. the auditor compared two identical placeholders and printed PASS.
```

### Every SAM3 command, audio extraction and the CPU preview render run on the main UI thread; the window shows 'Not Responding' for the duration and the close handler blocks on the same lock

- **Area:** threading / IPC  
- **Effort:** small · **User-facing:** yes

**Why it matters.** Clicking 'auto mask' the first time freezes the whole window for 30-45 s (up to 5 min on timeout); Windows greys it out and offers to kill it. Opening any video freezes the window while the audio track is transcoded. Closing the app mid-prompt appears to hang. None of this is a crash, but to a non-technical user it is indistinguishable from one.

**Evidence.**

```
src-tauri/src/commands.rs:1231 `pub fn sam3_init(` and :1251,1262,1273,1291,1306,1321,1344,1365,1381,1391 are all non-async `pub fn`; :1112 `pub fn extract_audio_from_video(`; :360 `pub fn apply_effect_stack(`. `grep -rn 'tauri::command(' src-tauri/src` finds no `#[tauri::command(async)]` opt-in. tauri-macros-2.6.3/src/command/wrapper.rs:50 defaults `execution_context: ExecutionContext::Blocking` and :266 maps that to kind `"sync"` (only `Async` gets `"sync_threadpool"`); wry-0.55.1/src/webview2/mod.rs ~:1021 calls `custom_protocol_handler(...)` inline inside the `WebResourceRequested` callback, and its responder at ~:1012 checks `std::thread::current().id() == main_thread_id` -- i.e. the UI thread. Durations: sam3_engine.rs:335-343 records cold sidecar starts of 29.7 s, 33.8 s and 44.3 s; :527-531 handshake timeout 300 s; :588-596 per-command timeouts 120 s (prompts) / 180 s (load_image) / 600 s (video_predictor); ffmpeg/mod.rs:1803 audio extract timeout 600 s. The guard is held for the whole call: commands.rs:1235 `let mut sam3_lock = lock_sam3(&state)?;` around `Sam3Engine::new`, :1194-1225 `with_sam3` holds it across `f(engine)`. src-tauri/src/lib.rs:86-95 the `CloseRequested` handler calls `state.sam3.lock()` on the event-loop thread, so clicking X during a SAM3 call waits for that call to finish or time out.
```

### The last recorded full gate on this tree FAILED (E2E), and no gate run is recorded for HEAD -- the handoff's 'all gated 10/10' cannot be shown for the final commit

- **Area:** verification  
- **Effort:** small · **User-facing:** no

**Why it matters.** The transport-strip commit touched DockLayout, defaultLayout, panelRegistry, AppLayout and Timeline; vitest and E2E are unproven on the exact tree that built the 0.2.0 installers (bundle/nsis 17:56:40).

**Evidence.**

```
scratchpad/gate_strip.log (started 2026-09-09 17:39:39, mtime 17:47:53): '[9/10] Playwright E2E suite  FAIL  392.4s ... 1 failed: tests/e2e/timeline.spec.ts:71 play/pause button toggles between Play and Pause' (old locator '.flexlayout__tab ... Animation length'). HEAD 274502f committed 17:50:01 (git log --date=iso), i.e. 2 min after the failing run ended; git show --stat HEAD lists tests/e2e/timeline.spec.ts (+10/-), src/components/__tests__/Timeline.test.tsx (+84), tests/e2e/dock-system.spec.ts. The spec now uses page.getByTestId("transport-strip") (timeline.spec.ts:76) and Timeline/index.tsx:154 has data-testid="transport-strip", so the fix is plausible -- but the only later log is build_v10.log (17:56:40, `tauri build`: prebuild+tsc+vite only). E2E takes ~6.5 min (gate_timeline.log: 408 s) so it cannot have run between 17:47:53 and 17:50:01. Handoff line 81: 'Fixed (all gated 10/10, committed)'.
```

---

## MINOR (46)

### 'Analyze Beats' writes keyframes to a hardcoded `intensity` parameter that ~90% of effects do not have

- **Area:** AudioPanel  
- **Effort:** small · **User-facing:** yes

**Why it matters.** Select Pixelate, press Analyze Beats: '48 beats -> keyframes applied', diamonds appear on the strip, nothing pulses. (And per finding 1 it would not export even when the param exists.)

**Evidence.**

```
AudioPanel/index.tsx:92-102 `paramKey: "intensity"`, then setKeyframesForTrack(selectedStackId, "intensity", ...) and status 'keyframes applied'. `grep -rl '"intensity"' src-tauri/src/effects | wc -l` -> 9 of 90 effect source files. useKeyframePlayback.ts:16-26 then injects an `intensity` value into params of an effect that ignores it (store/index.ts:825-830 updateStackParamsSilent).
```

### Command palette (Ctrl+Shift+P) is undiscoverable and is the only home of 'Close media' and window-docking commands; UI click sounds cannot be disabled

- **Area:** CommandPalette / useSoundManager  
- **Effort:** small · **User-facing:** yes

**Why it matters.** Non-technical users never learn the palette exists, so those actions are unreachable; every click makes a noise with no off switch.

**Evidence.**

```
CommandPalette.tsx:57-59 opens on Ctrl/Cmd+Shift+P; `grep -rn "Ctrl+Shift+P\|command palette" src/components` -> only a code comment in Toolbar.tsx:559. Palette-only commands: close-media (:93), toggle-edge-snap (:102), dock-left/right (:103,:109); no 'Close' item in the File menu (Toolbar.tsx:490-580). Sounds: useSoundManager.ts:8 `enabled = true`, setEnabled (:85) has no callers (grep); AppLayout.tsx:76-79 attaches clicks to all interactive elements.
```

### README tells the stranger the product is not ready, states no system requirements, and describes a stack that does not exist

- **Area:** Docs / release messaging  
- **Effort:** small · **User-facing:** yes

**Why it matters.** The GitHub README is the first thing a downloader reads; it currently tells them not to use the app and omits the one requirement (an NVIDIA GPU, ~11 GB of disk for SAM3) that decides whether the headline feature will work for them.

**Evidence.**

```
README.md:5 "> **Status:** Early development. Not yet ready for production use." README.md:28 "Segmentation | ONNX Runtime (`ort` crate) + SAM3" while `grep -n "^ort|onnx" src-tauri/Cargo.toml` -> nothing. No GPU/RAM/disk requirements anywhere in README (grep above). docs/SESSION-HANDOFF.md:200 "Version is still `0.1.0`" while package.json:4, tauri.conf.json:3 and Cargo.toml:3 all say 0.2.0. CHANGELOG.md:15-20 files the entire project history under 0.2.0 and lists "SAM3 segmentation engine skeleton (ONNX Runtime)".
```

### FFglitch section asymmetries: panel buttons enabled for stills (Toolbar guards them), and the 2 s preview moshes the raw source despite the 'applied after your effects' label

- **Area:** ExportPanel FFglitch section  
- **Effort:** small · **User-facing:** yes

**Why it matters.** With a still loaded, the panel offers a preview/export that the menu equivalent refuses; the preview shows a different picture from what the export will produce.

**Evidence.**

```
ExportPanel/index.tsx:347-353 handleFfglitchExport has no isVideo check; buttons :711-716 and :741-764 disabled only on busy/!mediaInfo/!filePath. Toolbar.tsx:357-364 guards with 'FFglitch needs a video ... Use File > Animate as Video first'. Preview: ExportPanel:330-345 `previewFfglitch(filePath, ffglitchMode, 1.0, 2.0)` on the source path, ignoring the stack and in/out, under the label at :683 'Bitstream datamosh -- applied after your effects'. What preview_ffglitch does with a PNG input (commands.rs:2645-2660 ffmpeg -ss/-t trim) was not run, so the exact failure text is unverified.
```

### Panel FFglitch export ignores the Format, Quality, Resolution and Processing chips above it

- **Area:** ExportPanel handleFfglitchExport  
- **Effort:** small · **User-facing:** yes

**Why it matters.** Select 4K + Best + <=1080p processing, press 'Export FFglitch' -> source resolution at default quality; the chips look global but only govern 'Export Video'.

**Evidence.**

```
ExportPanel/index.tsx:375-388 exportVideo(filePath, stack, { maskB64, codec, fps, audioBakeJson, watermark, trimStart, trimEnd, includeAudio, outputPath }) -- no width/height/format/quality/processingScale, which handleExport does pass at :268-285. Output container is decided by the Save dialog (:363-370 MP4/AVI).
```

### Watermark image must be typed as a file path; no picker

- **Area:** ExportPanel watermark  
- **Effort:** small · **User-facing:** yes

**Why it matters.** Non-technical users do not know or type Windows paths; the only image-watermark route is effectively developer-only.

**Evidence.**

```
ExportPanel/index.tsx:~848-862 `<input type="text" value={watermark.imagePath ?? ""} placeholder="Image path">`. Contrast LUTPanel/index.tsx:21-27 which uses the native `open()` dialog for custom LUTs. Rust validates the path (commands.rs:694-695) so a typo just fails.
```

### Active LUT tile is not marked in the gallery

- **Area:** LUT panel  
- **Effort:** small · **User-facing:** yes

**Why it matters.** Since 21457a1 clicking a tile swaps the look, so exactly one is live and nothing shows which.

**Evidence.**

```
src/components/LUTPanel/index.tsx:97 tile className has only hover/focus-visible/active:scale styling; grep for isActive/activeLut/selected/aria-pressed/aria-current -> none. Handoff 'Open' item 2.
```

### Developer-facing and inconsistent copy in user-visible surfaces (LUT paths, three names for one panel, onboarding promises)

- **Area:** LUTPanel / EffectStack / OnboardingModal  
- **Effort:** small · **User-facing:** yes

**Why it matters.** First-run text sends users looking for a 'Layers' panel that does not exist and for PNG export in the wrong place; the LUT panel talks about source-tree directories.

**Evidence.**

```
LUTPanel/index.tsx:77-83 tells users bundled presets 'live in public/lut/ for dev builds and resources/lut/ for production installers'. Panel naming: panelRegistry.ts:27 tab 'Stack'; EffectStack/index.tsx:75 header 'Inspector'; OnboardingModal.tsx:74 'Add and stack effects in the Layers panel'. OnboardingModal.tsx:77 'Export to PNG, JPG, GIF, or MP4' -- PNG/JPG exist only as File > Save Image (Toolbar.tsx handleSaveImage; lib/tauri.ts:366-385), the Export panel format list (ExportPanel/index.tsx:56-70) has no single-image PNG/JPG.
```

### Preview header buttons are mislabeled and the manual CPU/GPU toggle survives the 'choice is gone' redesign

- **Area:** PreviewViewport header  
- **Effort:** small · **User-facing:** yes

**Why it matters.** Three controls in the most-looked-at bar do something other than what they say.

**Evidence.**

```
PreviewViewport.tsx:1311-1318 button titled 'Aspect ratio' calls setZoom(1); :1319-1325 'More options' (more_vert) calls toggleFullscreen. Status pill :1256-1266 still toggles useCpuPreview ('Click to switch to 100% exact Rust CPU preview') although docs/PRODUCT-REVIEW.md:39-45 says the choice is gone. The auto-exact effect :1197-1214 deliberately excludes useCpuPreview from its deps, so after a manual click the label logic at :1221-1237 can show 'REFINING…' with no render pending.
```

### 'Use Proxy' checkbox and 'Lock Aspect Ratio' chips are wired to nothing

- **Area:** ProxyPanel / ExportPanel  
- **Effort:** small · **User-facing:** yes

**Why it matters.** Unticking 'Use Proxy' does not restore the original; picking 9:16 changes no export.

**Evidence.**

```
proxyEnabled readers: store/index.ts:288,612,1229 and ProxyPanel.tsx:7,49-55 only; the preview reads proxyUrl, which ProxyPanel.tsx:37 sets unconditionally. aspectRatioLock/aspectRatio readers: ExportPanel/index.tsx:135-137,570-604 (UI) and store/index.ts:284-285,610-611,987-988 only; not passed to exportVideo (ExportPanel:268-285; tauri.ts:406-428 has no aspect option).
```

### Uninstalling leaves the 6.2 GB add-on behind; no in-app removal exists

- **Area:** SAM3 add-on / uninstall  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** A non-technical user who uninstalls keeps 6 GB in a hidden dot-folder with no UI to reclaim it.

**Evidence.**

```
src-tauri/src/sam3_engine.rs:169-174 sam3_addon_dir = ~/.moshdither/sam3; src-tauri/src/sam3_addon.rs:134-146 checkpoint_path = ~/.moshdither/models/sam3/sam3.pt. On this machine: du -sh ~/.moshdither -> 6.2G (sam3-bridge.exe 3,061,917,014 B; sam3.pt 3,450,062,241 B). tauri.conf.json has no nsis/installerHooks key; sam3_addon.rs public fns are only sam3_addon_status and sam3_addon_install. Handoff line 26-27 recorded the uninstaller-leaves-sidecar trap.
```

### The ~6 GB SAM3 add-on download has no cancel

- **Area:** Sam3Setup / sam3_addon.rs  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** A user who starts a 6 GB download on the wrong connection can only close the app.

**Evidence.**

```
Sam3Setup.tsx:147-173: 'Download and install SAM3' then a progress bar only; no cancel control. `grep -n "cancel\|Cancel\|abort" src-tauri/src/sam3_addon.rs` -> no matches.
```

### Working files under %TEMP%\moshdither-studio are never pruned by the app

- **Area:** Temp hygiene  
- **Effort:** small · **User-facing:** no

**Why it matters.** Proxies and per-mode datamosh previews of every clip a user ever opens accumulate until Windows Disk Cleanup runs; small per file, unbounded over months.

**Evidence.**

```
Writers: ffmpeg/mod.rs:210-217 (`proxy/<name>_proxy_<w>p.mp4`), ffmpeg/mod.rs:368-370 (`animated-stills/`), commands.rs:1132-1134 (`extracted-audio/<hash>.wav`), commands.rs:2209 (`luts/`), commands.rs:2613-2615 (`mosh-preview/mosh-preview-<hash>.mp4`, one per source+mode+start+length, cached forever by design at 2635-2642). `grep -n remove_dir_all src-tauri/src/commands.rs` -> nothing; the only removals are individual intermediates. Local state today: `du -sh $TEMP/moshdither-studio` -> 0 (dev machine cleaned by hand).
```

### Developer diagnostics ('Verify') panel is reachable from Edit > Panels and 'Show All' despite being hidden from the rail

- **Area:** Toolbar Edit menu / DockLayout.addPanel  
- **Effort:** small · **User-facing:** yes

**Why it matters.** A QA tool sits one menu away for every user, contradicting the stated intent in the store comment; 'Show All' also docks Tracks and Proxy Media.

**Evidence.**

```
store/index.ts:588-592 `verify: false` with comment 'Hidden from the panel rail'. Toolbar.tsx:600-640 maps every PANEL_REGISTRY entry (panelRegistry.ts:97-110 includes verify) into Edit > Panels, and 'Show All' (Toolbar.tsx:~644-652) adds all of them. DockLayout.tsx:148-169 addPanel has no panelVisibility check. View > Advanced > 'Diagnostics Panel' (Toolbar.tsx:~760-772) also toggles it. VerificationPanel/index.tsx:17-28 runs the effect self-test suite.
```

### Panel drag-rearrangement and the new keyframe diamonds / in-out drag handles have no E2E coverage; keyframes are covered only under jsdom

- **Area:** UI interaction coverage  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** Acceptable for a desktop app provided a manual pre-release checklist includes 'drag a panel', 'drag the in/out handles', 'right-click a diamond'. The jsdom tests pin the logic; they cannot see hit-target size, z-order, or pointer capture in a real WebView.

**Evidence.**

```
grep -rn -i 'keyframe|diamond|in handle|out handle' tests/e2e/*.spec.ts -> no matches. src/components/__tests__/Timeline.test.tsx lines 220-287 cover 'draws one diamond per distinct time', 'clicking a diamond jumps the playhead', 'right-clicking a diamond deletes', 'dragging the in handle ...', 'cannot be dragged past the out point' -- jsdom with synthesized pointer events. SESSION-HANDOFF: 'the drop itself is HTML5 drag-and-drop, which synthetic events cannot trigger -- verifying a rearrange end-to-end needs a real mouse.'
```

### Developer-facing strings and toggles remain reachable from the shipped UI

- **Area:** UX copy / developer surface  
- **Effort:** small · **User-facing:** yes

**Why it matters.** Against the owner's own bar of "no developer toggles, no technical error strings"; each of these is what a non-technical user sees on a broken or unusual install.

**Evidence.**

```
commands.rs:2705-2707 "...build the sidecar with `npm run build:mosh-sidecar`"; commands.rs:2718 "FFglitch binaries (ffgac / ffedit) not found. Download them from ffglitch.org and place in src-tauri/bin/" (reachable because ffmpeg/mod.rs:135-158 `locate_binary` falls back to a bare name that fails the `exists()` test). sam3_bridge.py:340-356 instructs the user to create a Hugging Face token and "Set HF_TOKEN" if the checkpoint is missing. Toolbar.tsx:773-783 View -> Advanced -> "Diagnostics Panel" ("Effect self-test suite for diagnosing a broken install"). MaskPanel.tsx:145,167,220 and Sam3Setup.tsx:84 render raw error strings into the status bar/panel.
```

### A fresh clone still cannot build without a Python toolchain: mosh-cli is required by prebuild but not fetchable, and sam3_repo needs a hand patch

- **Area:** build reproducibility  
- **Effort:** medium · **User-facing:** no

**Why it matters.** Release builds depend on binaries that live only on this machine; a second builder cannot reproduce 0.2.0.

**Evidence.**

```
scripts/verify-external-bins.mjs:227-235 fails prebuild when bin/mosh-cli-<target>.exe is missing ('Build it with: npm run build:mosh-sidecar'); src-tauri/bin/SIDECARS.json 'binaries' keys are ffmpeg, ffprobe, ffgac, ffedit only (lines 24-47); scripts/fetch-external-bins.mjs has no mosh-cli entry (its only 'mosh' match is the tmpdir prefix at line 153). .gitignore:94 ignores packages/python-backend/sam3_repo; docs/SAM3_SETUP.md:24-41 describes the manual weights_only=False patch. Handoff line 182-191 'HALF FIXED'. Also .gitignore:27 ignores src-tauri/Cargo.lock (accepted, handoff line 170-171).
```

### $TEMP/moshdither-studio accumulates proxies, previews, audio and LUT copies with no pruning

- **Area:** data hygiene / temp files  
- **Effort:** small · **User-facing:** no

**Why it matters.** Long-term users accumulate gigabytes of proxies and cached previews in TEMP with nothing in the app that clears them.

**Evidence.**

```
src-tauri/src/ffmpeg/mod.rs:210-217 proxies `{stem}_proxy_{width}p.mp4`; :366-370 animated-stills; src-tauri/src/commands.rs:1132-1136 extracted-audio/{hash}.wav; :2613-2632 mosh-preview/mosh-preview-{key}.mp4 cached per (source, mode, start, seconds) and reused `if out.exists()`; :2209-2212 luts/ copies. `grep -rn moshdither-studio|remove_dir_all src-tauri/src` shows removal only for add-on staging (sam3_addon.rs:449) and test fixtures; no startup or exit cleanup.
```

### ~/.moshdither (6 GB add-on + weights + logs) survives uninstall and cannot be removed from the app

- **Area:** data hygiene / uninstall  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** Uninstalling leaves ~6 GB in the user's home with no UI or uninstaller step to reclaim it.

**Evidence.**

```
src-tauri/src/sam3_addon.rs:169-174 `sam3_addon_dir()` -> `~/.moshdither/sam3`; :141-152 checkpoint at `~/.moshdither/models/sam3/sam3.pt`; crash.rs:38-41 logs at `~/.moshdither/logs`. `grep -rn -i sam3_addon_remove|removeAddon|uninstall src src-tauri/src` -> none; src-tauri/tauri.conf.json has no `nsis`/`installerHooks` key. docs/SESSION-HANDOFF.md:26-28 "the NSIS uninstaller does NOT remove sam3-bridge.exe, so a stale sidecar survives an uninstall/reinstall cycle."
```

### Recovery dialog says "An unsaved session was found" on every launch, even after a clean Save and close

- **Area:** data-safety / autosave UX  
- **Effort:** small · **User-facing:** yes

**Why it matters.** Every launch after any real session opens a modal implying lost work; users learn to click Discard reflexively, which is the one click that actually destroys a recoverable session.

**Evidence.**

```
src/components/AppLayout.tsx:42 `const [showRecovery, setShowRecovery] = useState(!!autoSave);`. `clearAutoSave()` is only called at AppLayout.tsx:380 (Discard button). `onCloseRequested` handler (AppLayout.tsx:115-125) only confirms and never clears; `saveProject` (useProject.ts:36-65) never clears. useProjectSession.ts:96-121 writes the autosave every 5 s whenever `filePath || previewDataUrl || effectStack.length > 0 || audioFilePath`.
```

### Project file is written in place, not atomically

- **Area:** data-safety / project save  
- **Effort:** small · **User-facing:** yes

**Why it matters.** A crash or power loss during a save (up to the 10 MB cap at commands.rs:2101) truncates the user's only copy of the project.

**Evidence.**

```
src-tauri/src/commands.rs:2152-2153 `let valid_path = validate_project_path(&path, true)?; std::fs::write(&valid_path, contents).map_err(|e| e.to_string())`. Contrast src-tauri/src/presets.rs:111 "Best-effort cleanup so a failed rename does not leave a stray temp file" (temp + rename pattern used for presets).
```

### Every Ctrl+S is Save As; the app never remembers the current project path

- **Area:** data-safety / save workflow  
- **Effort:** small · **User-facing:** yes

**Why it matters.** A file-picker on every save discourages saving; MoshPro-class users expect Save to overwrite the open project silently and Save As to prompt.

**Evidence.**

```
src/hooks/useProject.ts:36-40 `const path = await save({ filters: [...], defaultPath: "project.moshdither" });` on every call; `grep -n -i projectPath|currentProject|projectFilePath src/store/index.ts src/hooks/useProject.ts` -> no matches. useKeyboardShortcuts.ts:196 `saveProject();` bound to the save shortcut.
```

### Seven tomato datamosh modes still broken in the backend, hidden from the dropdown

- **Area:** datamosh  
- **Effort:** medium · **User-facing:** no

**Why it matters.** Feature parity gap versus MoshPro's mode list; safe for users because hidden, but the root cause (vendored Tomato corrupting the AVI beyond decode on real footage) is unfixed.

**Evidence.**

```
src/components/ExportPanel/index.tsx:76-81 FFGITCH_MODES comment: 'bloom, overlap, jiggle, void, reverse, invert, random -- the tomato family ... the re-encode writes 0 bytes'; list contains 25 ids, none of those seven. packages/python-backend/mosh_cli.py:230 still lists all eight tomato modes. Failure path: src-tauri/src/commands.rs:2782-2797 removes a 0-byte output and returns 'FFglitch failed: <stderr>'. Handoff line 102-106.
```

### Mosh preview's trim step runs ffmpeg with no timeout on the async runtime, and the preview queues silently behind any running export

- **Area:** datamosh preview  
- **Effort:** small · **User-facing:** yes

**Why it matters.** A malformed file that hangs ffmpeg leaves the preview spinner forever with no cancel; clicking Preview during an export shows a spinner that will not move until the export finishes.

**Evidence.**

```
src-tauri/src/commands.rs:2598 `pub async fn preview_ffglitch(`; :2644-2660 `crate::proc::command(&ffmpeg)...output()` -- plain `.output()`, no `output_with_timeout`, no cancel, executed directly on the tokio worker; :2748-2752 the subsequent `apply_ffglitch` awaits `export_semaphore.acquire_owned()` (one permit, shared with `export_video` :711). src/components/ExportPanel/index.tsx:333-345 `handleMoshPreview` sets `moshPreviewBusy` and only clears it in `finally` after the command resolves.
```

### 'Animate to Video' is still reachable only via File menu; Export panel gives no hint

- **Area:** discoverability  
- **Effort:** small · **User-facing:** yes

**Why it matters.** The review calls still-photo animation the product's headline capability (98/99 effects animate a still).

**Evidence.**

```
grep -i animate src/components/ExportPanel/index.tsx -> only a comment at line 44; AnimateAsVideoModal referenced from src/components/Toolbar.tsx and AppLayout.tsx only. PRODUCT-REVIEW.md:71-73 recommendation ('Save Image' and 'Animate to Video' in the Export panel) not applied.
```

### Raw FFmpeg/mosh-cli stderr and internal timeout strings are the user's only error message, rendered in a one-line status bar

- **Area:** error surfacing  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** The stated audience is non-technical. 'FFmpeg exited while writing frame 12/300 (The pipe has been ended): [libx264 @ 000001F...' in a status bar is not something they can act on; the sentence they need ('the output drive is full', 'this format does not support that size') is buried or absent.

**Evidence.**

```
src-tauri/src/ffmpeg/mod.rs:1361-1366 `"FFmpeg exited while writing frame {}/{} ({}): {}"` with up to 2000 chars of stderr; :1416-1420 `"FFmpeg encode failed (exit {}): {}"` 2000 chars; :1519-1522 `"Process timed out after {:?}"`; src-tauri/src/commands.rs:2789-2792 `"FFglitch failed: {}"` with the full stderr; src-tauri/src/sam3_engine.rs:576-579 `"SAM3 IPC timed out after {:?} for command '{}': {}"`. Frontend passes them straight through: src/components/ExportPanel/index.tsx:300 `Export failed: ${err.message}`, :412; src/hooks/useProject.ts:64 `Save failed: ${err}`, :143 `Open failed: ${err}` (a corrupt .moshdither file yields `SyntaxError: Unexpected token ...` from :78 `JSON.parse`); src/components/StatusBar.tsx:47 `<span data-testid="status-message">{statusMessage}</span>`.
```

### No gate installs or launches the NSIS/MSI bundle; app-smoke runs the unbundled exe from target/release

- **Area:** installer coverage  
- **Effort:** human-step · **User-facing:** yes

**Why it matters.** Acceptable for a desktop app -- installers are normally verified by hand -- but the handoff already documents two sessions lost to a stale install and an uninstaller bug found only manually. The pre-release checklist must include install-from-bundle on a clean profile; nothing automated stands in for it.

**Evidence.**

```
evals/regression.ps1:65: '$exe = Join-Path $PSScriptRoot .. src-tauri target release moshdither-studio.exe'. scratchpad build_v10.log 17:56:40: 'Finished 2 bundles at: ...bundle/msi/MoshDither Studio_0.2.0_x64_en-US.msi' and '...bundle/nsis/MoshDither Studio_0.2.0_x64-setup.exe' -- nothing in evals/ or tests/ references either. SESSION-HANDOFF: 'the NSIS uninstaller does NOT remove sam3-bridge.exe ... Check for it by hand' and 'A STALE INSTALL WILL WASTE YOUR SESSION. This has now cost two of them.'
```

### SESSION-HANDOFF.md carries contradictory or stale ledger lines

- **Area:** ledger accuracy  
- **Effort:** small · **User-facing:** no

**Why it matters.** The handoff is the only pass-down artefact and it currently sends the next session chasing closed items and trusting wrong facts.

**Evidence.**

```
Line 44 'passes all 9 gates' vs evals/regression.ps1 line 64 adds a 10th (app-smoke) and line 81 says 10/10. Line 200 'Version is still 0.1.0' vs tauri.conf.json:3, package.json:4, Cargo.toml:3 all 0.2.0 (and line 45 says 0.2.0). Line 179-181 'src-tauri/target/release/sam3-bridge.exe is a STALE 297 MB CPU-only build from 2026-08-21 ... Do not copy it' -- on disk it is 3,061,917,014 bytes dated Sep 5 11:06, byte-size-identical to the good sidecar in src-tauri/bin. Lines 173-198 ('What is not.') still say the updater is 'configured but dead' although it has been removed (lib.rs:79-82). 'Open' item 4 'Mask controls untested' -- tests/e2e/mask-panel.spec.ts has 6 tests and tests/e2e/sam3-mask-flow.spec.ts 1 test against a mocked SAM3 (commit 3ccf6e9); real-model quality remains untested (line 146-148). 'Open' item 5 (composite.overlay placeholder) is already covered by REQUIRES_SELECTION in src/utils/effectConverter.ts:1076-1085. PRODUCT-REVIEW.md:44-45 says previewAutoExact 'turns it off for anyone who wants the old manual behaviour' but setPreviewAutoExact is called from no component (grep src/components -> none).
```

### SAM licence acceptance links to the gated Hugging Face page instead of showing the licence; notices claim it "is presented before download"

- **Area:** licensing / add-on UX  
- **Effort:** small · **User-facing:** yes

**Why it matters.** The product targets non-technical users who will not find a licenses folder, and the linked HF repo is gated so the link shows an access wall rather than the Agreement; the in-app claim overstates what happens.

**Evidence.**

```
src/components/Sam3Setup.tsx:130-145: checkbox label links `href="https://huggingface.co/facebook/sam3.1"` and says "The full text is installed with the app in the licenses folder." No component renders licenses/SAM-LICENSE.txt. THIRD-PARTY-NOTICES.md:110 "is presented before download"; scripts/publish-sam3-addon.mjs:233 "shown in the app".
```

### Installer licence summary says SAM materials are "NOT included in this installer", but Meta's sam3 source tree (SAM License, 74 MB, plus tests/examples/.github/__pycache__) is bundled and used by the installed sidecar

- **Area:** licensing / installer contents  
- **Effort:** small · **User-facing:** no

**Why it matters.** The licence summary a user sees at install time misstates what is on disk, and 74 MB of Meta code plus stray __pycache__ ships in every installer whether or not the user ever enables SAM3.

**Evidence.**

```
src-tauri/tauri.conf.json:65 `"../packages/python-backend": "packages/python-backend"` bundles the directory wholesale; packages/python-backend/sam3_repo is gitignored but present (`du -sh` -> 74M; `head licenses` -> `SAM License / Last Updated: November 19, 2025`; contains .github/ test/ examples/ sam3.egg-info/). Last release build copied it: `src-tauri/target/release/packages/python-backend/sam3_repo/` exists (74M). src-tauri/src/sam3_engine.rs:306-315 `resolve_sam3_repo` prefers `resource_dir()/packages/python-backend/sam3_repo`, and lines 379-381 pass it as `SAM3_REPO` to the installed sidecar; sam3_bridge.py:154-155 `sys.path.append(str(SAM3_REPO))`. licenses/INSTALLER-LICENSE.txt section 4: "NOT included in this installer." The SAM License copy does ship (licenses/SAM-LICENSE.txt via tauri.conf.json:68), so 1.b.i is met; the summary is inaccurate and the sidecar already carries sam3 (scripts/build-sam3-sidecar.py:74 `REQUIRED_MODULES = ("torch", "iopath", "sam3")`, bridge falls back to `_MEIPASS` at sam3_bridge.py:147-149).
```

### Material Symbols font (Apache-2.0) and all npm/Rust dependency notices are absent from the shipped notices

- **Area:** licensing / notices completeness  
- **Effort:** medium · **User-facing:** no

**Why it matters.** Apache-2.0 section 4 and MIT both require their notices to accompany redistribution; the frontend bundle and the Rust binary redistribute dozens of such packages with none.

**Evidence.**

```
`ls public/fonts` -> material-symbols-outlined-0..6.ttf + .css, produced by scripts/subset-material-symbols.mjs:237 from `https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined`. `grep -i material|apache|OFL THIRD-PARTY-NOTICES.md` -> only the SAM3-bundle sentence at line 125. `grep -i license-checker|cargo-about|cargo-deny package.json` -> nothing. THIRD-PARTY-NOTICES.md lists only FFmpeg, FFglitch, mosh-cli, SAM weights and the sidecar.
```

### Datamosher-Pro (Akash Bora, MIT) code is shipped and compiled into mosh-cli.exe without its copyright/licence notice; Tomato and pymosh authors are not named in the notices

- **Area:** licensing / vendored Python  
- **Effort:** small · **User-facing:** no

**Why it matters.** MIT requires the copyright notice to accompany copies; three authors' notices are missing or unnamed in the installer and entirely absent from the frozen sidecar.

**Evidence.**

```
.gitmodules:15 `url = https://github.com/Akascape/Datamosher-Pro.git` (submodule is an empty gitlink: `git ls-files -s references/Datamosher-Pro` -> 160000 1e0f82d..., directory empty). packages/python-backend/DatamoshLib/Original/classic.py:1 `#Author: Akash Bora`; FFG_effects/basic_modes.py:1 and external_script.py:1 same; `ls DatamoshLib/Original/` -> classic.py classic_new.py pymodes.py repeat.py __pycache__ (no LICENSE). Upstream README (WebFetch github.com/Akascape/Datamosher-Pro): "The main app files written by the Author is MIT licensed". THIRD-PARTY-NOTICES.md:82 says only "vendored `DatamoshLib` and `pymosh`" -- no author, copyright or licence named; grep for ravel|friedl|bora in the notices -> none. DatamoshLib/Tomato/LICENSE.txt (MIT, Kaspar Ravel) and pymosh/LICENSE.txt (MIT, Joe Friedl) ship inside the resource dir but nothing travels inside mosh-cli.exe.
```

### Mask undo covers only Clear/Invert; a brush or eraser stroke cannot be undone

- **Area:** mask editing  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** A misplaced stroke on a detailed hand-painted mask is unrecoverable.

**Evidence.**

```
src/components/ManualMaskEditor.tsx:18-26 'One-step undo for the whole-mask operations'; setUndoableMask called only at lines 56 (Clear) and 77 (Invert); brush/eraser tools at lines 132-133, 183-186 take no snapshot. Handoff line 118.
```

### `mosh-verify.exe`, a 10 MB console test harness, ships inside the installer and is undocumented in the third-party notices

- **Area:** packaging — bundle contents  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** A non-technical user browsing the install folder finds an unexplained second executable that pops a console window when double-clicked — at odds with the stated 'no console windows, no developer toggles' goal — and it adds ~10 MB to every download.

**Evidence.**

```
7z listing of the 0.2.0 NSIS: `2026-09-09 17:54:04 ....A 10480640 mosh-verify.exe`. src-tauri/Cargo.toml:51-53 `[[bin]] name = "mosh-verify" path = "src/bin/mosh_verify.rs"` with the comment "Standalone CLI for running effect verification without the Tauri runtime"; Tauri bundles every [[bin]] target. `grep -n windows_subsystem src-tauri/src/bin/mosh_verify.rs` -> no match (console subsystem). `grep -rn -i mosh-verify THIRD-PARTY-NOTICES.md docs/SESSION-HANDOFF.md scripts/archive-build.mjs src-tauri/tauri.conf.json | grep -i -E 'install|bundle|ship'` -> nothing.
```

### Release builds are not reproducible from the repo: Cargo.lock is gitignored, mosh-cli has no pinned hash and must be built with local Python/PyInstaller, and the SAM3 sidecar needs a hand-patched clone nothing automates

- **Area:** packaging — reproducibility  
- **Effort:** medium · **User-facing:** no

**Why it matters.** A second machine (or the owner after a disk loss) cannot rebuild the shipped 0.2.0 binary or the shipped datamosh sidecar bit-for-bit, and the SAM3 sidecar whose hash is compiled into the app (EXPECTED_SIDECAR_SHA256) cannot be regenerated without manual steps — so any future sidecar fix forces an app release whose sidecar is built by hand.

**Evidence.**

```
.gitignore:27 `/src-tauri/Cargo.lock`; `git ls-files src-tauri/Cargo.lock` -> empty while the file exists locally (152,163 B). src-tauri/bin/SIDECARS.json `binaries` pins only ffmpeg, ffprobe, ffgac, ffedit (no mosh-cli); all five .exe are gitignored (.gitignore:70) and `git ls-files src-tauri/bin` -> only SIDECARS.json. scripts/verify-external-bins.mjs:227-234 fails a fresh clone with "tauri.conf.json lists bin/mosh-cli but the binary is missing ... Build it with: npm run build:mosh-sidecar" (python scripts/build-mosh-sidecar.py, PyInstaller + numpy). docs/SESSION-HANDOFF.md:170-171 "Cargo.lock is gitignored, so release builds are not byte-reproducible. Pre-existing choice"; :186-191 "STILL OPEN: packages/python-backend/sam3_repo is gitignored and needs a PATCHED clone ... Nothing automates that yet." package-lock.json IS tracked (`git ls-files package-lock.json`).
```

### End users get developer instructions and no install guidance: the add-on error tells them to run `npm run build:sam3-sidecar`, and the README has no end-user section (WebView2 bootstrapper download, SmartScreen, ~6 GB SAM3 download, ~/.moshdither disk use)

- **Area:** packaging — user-facing install experience  
- **Effort:** small · **User-facing:** yes

**Why it matters.** The stated audience is non-technical MoshPro users. An npm command in an error dialog and a README with only developer prerequisites mean the first thing many users read after a failure is aimed at the wrong person.

**Evidence.**

```
src-tauri/src/sam3_addon.rs:325-327 error text: "Could not reach the SAM3 asset list at {url}: {e}\n\nIf this release has not published the add-on yet, build the sidecar locally with `npm run build:sam3-sidecar` instead."; :330-331 `"SAM3 asset list unavailable: {e}"` surfaces raw reqwest status text. src/components/Sam3Setup.tsx:106 "AI segmentation needs a one-time download of about 6 GB" is the only place the size is stated. `grep -n -i -E 'webview2|6 GB|add-on|\.moshdither|GB' README.md` -> no matches; README.md:32-36 'Prerequisites' lists Rust/Node only. tauri.conf.json `webviewInstallMode: { type: "downloadBootstrapper", silent: true }` (installer needs internet). sam3_engine.rs:169-173 add-on lives in `~/.moshdither/sam3` alongside `~/.moshdither/models` (3.06 GB + 3.45 GB on this machine).
```

### WebGL context leak is documented in source as deliberately unfixed; human verification of the black preview in a real build still not recorded

- **Area:** preview  
- **Effort:** human-step · **User-facing:** yes

**Why it matters.** Browsers cap live contexts at 16 and force-lose the oldest; repeated Preview dock/undock can still black the preview, and no recorded human run rules it out.

**Evidence.**

```
src/engine/webgl2/WebGLContext.ts:176-191: 'Deliberately NOT calling WEBGL_lose_context.loseContext() here ... The underlying leak is real ... Left leaking until'. Handoff line 235-241 and 'Open' item 1 (two hand recipes, 'wants your hands, not mine').
```

### Save/Open Project via the File menu (added 17:01 in 9666c04) has no test that reaches it through the UI; only the hook is unit-tested with a mocked dialog

- **Area:** project save/open coverage  
- **Effort:** small · **User-facing:** yes

**Why it matters.** The bug this commit fixed lived in the wiring between the hook, the store token and AppLayout -- precisely the seam a hook-level test with mocked dialogs cannot see. A regression of 'Open Project shows the previous media' would pass every gate.

**Evidence.**

```
grep -rln 'Save Project|Open Project|saveProject|openProject' tests/e2e src/**/*.test.* -> only src/hooks/useProject.test.ts, which has vi.mock("@tauri-apps/plugin-dialog", () => ({ open: vi.fn(), save: vi.fn() })) at lines 9-14 and vi.mock("@tauri-apps/api/core"). git show 9666c04 message: 'OPEN PROJECT NEVER LOADED THE MEDIA ... refreshPreview lives in AppLayout ... there is now a mediaReloadToken in the store'. Both e2e mocks answer 'plugin:dialog|open' with a canned .mp4 path.
```

### release.yml is dead and would fail if it ever ran: it passes a flag the overlay script does not recognise and requires the 3 GB sidecar that the installers cannot carry

- **Area:** release mechanics — CI workflow  
- **Effort:** small · **User-facing:** no

**Why it matters.** Harmless today only because Actions is off; it is misleading as documentation of the release path and would break the moment anyone enables it.

**Evidence.**

```
.github/workflows/release.yml:137 `run: node scripts/enable-sam3-sidecar.mjs --release`; scripts/enable-sam3-sidecar.mjs only checks `--no-sidecar` (:34) and `--disable-updater` (:80), and exits 1 at :39-53 when no `sam3-bridge-*` is in src-tauri/bin. release.yml:129-132 builds the sidecar and :159 runs tauri-apps/tauri-action@v1 to bundle it — contradicting sam3_engine.rs:164-168 (WiX/NSIS reject >2 GiB). Actions never runs on this account (SESSION-HANDOFF.md:29-34).
```

### CHANGELOG.md and README describe a product that does not match the tree

- **Area:** release notes / docs  
- **Effort:** small · **User-facing:** yes

**Why it matters.** These are the user-facing release notes and the repo front page for a first public release.

**Evidence.**

```
CHANGELOG.md [Unreleased] is empty while git log fa8ff37..HEAD -> 18 commits after the 0.2.0 docs commit (feat(timeline) transport strip, feat(datamosh) 34 modes preview, feat(presets), feat(preview) auto-exact, fix(datamosh) mosh-cli sidecar). The 0.2.0 entry never mentions the SAM3 add-on, mosh-cli or the transport strip (grep -i 'add-on|mosh-cli|transport' -> none) but lists 'SAM3 segmentation engine skeleton (ONNX Runtime)', 'Spout GPU output stub (Windows)' and 'SpoutSender unused fields' -- grep -rli spout src src-tauri/src -> none; grep 'ort\b|onnx' src-tauri/Cargo.toml -> none. README.md:28 'Segmentation | ONNX Runtime (`ort` crate) + SAM3'. CHANGELOG also lists 'Track panel UI' as a feature (see Tracks finding).
```

### Dead updater surface: capability grants updater:default and two updater commands are registered while the plugin is deliberately not initialised

- **Area:** security / IPC surface  
- **Effort:** small · **User-facing:** no

**Why it matters.** Two webview-callable commands that can only error, plus a permission grant for a plugin that does not exist; harmless today but it is exactly the kind of half-removed feature that gets re-enabled by one side only.

**Evidence.**

```
src-tauri/capabilities/default.json:31 `"updater:default"`. src-tauri/src/lib.rs:97-103 comment: "The updater plugin is NOT registered, and must not be while `plugins.updater` is absent from tauri.conf.json". lib.rs:133-134 still registers `check_update, install_update`; commands.rs:2230-2262 both call `app.updater()`, which fails without the plugin. tauri.conf.json:71 `"createUpdaterArtifacts": false`, no `plugins` block.
```

### Public-repo hygiene: owner's Windows username and working notes are tracked

- **Area:** security / repository hygiene  
- **Effort:** small · **User-facing:** no

**Why it matters.** Not a security exposure (no secrets found), but a public repository with local paths, agent scratch files and a stale secret-scan baseline reads as unfinished and leaks the maintainer's local layout.

**Evidence.**

```
`git grep -l -i richk -- ':!node_modules' ':!package-lock.json'` -> 5 files: HANDOFF.md, src/components/__tests__/Timeline.test.tsx, tools/audit_crew.py (+2 under docs/ and evals/reports/). Tracked at repo root: mcp_config.json, body.txt, task.md, references.code-workspace, CONTEXT.md, HANDOFF.md (`git ls-files` confirms). .secrets.baseline:267-268 still allowlists a "Base64 High Entropy String" in src-tauri/tauri.conf.json that no longer exists (the removed updater pubkey).
```

### cargo-test's functional suite passes at a 90 % threshold (up to 4 of 49 checks may fail), and 6 of its checks return Ok when the thing they check is absent

- **Area:** src-tauri/src/effects/functional_tests.rs  
- **Effort:** small · **User-facing:** no

**Why it matters.** A regression that breaks up to four effects' default render, or removes ffgac/ffedit from the bundle, keeps gate 6 green. The per-effect unit tests elsewhere reduce the exposure, but the suite named 'functional' is more lenient than it reads.

**Evidence.**

```
functional_tests.rs:713-720: 'let pass_rate = report.passed as f64 / report.total_tests as f64; assert!(pass_rate > 0.9, ...)'. 49 checks counted (3 io + 4 registry + 13 apply + 2 stack + 3 mask + 14 animate + 3 env + 2 file io + 2 video + 1 sam3 + 2 ffglitch). Always-Ok on absence: line 444 'Err(_) => Ok("ffgac not available (optional)")', 459 same for ffedit, 484-486 and 530-532 '_ => return Ok("Skipped: ffmpeg not available")', 583-596 sam3_availability_check Ok either way, 600 'Ok("Python not on PATH (FFglitch bridge will not work)")'. Only mosh_cli_check (641-645) fails loudly. SESSION-HANDOFF 'test-all 49/49' is a mosh-verify run, not this gate.
```

### ffmpeg_binary() can never return Err (PATH fallback), so every 'SKIP: no ffmpeg' guard in the Rust export/decode tests is unreachable and a clone without src-tauri/bin would test a system ffmpeg instead of the bundled one

- **Area:** src-tauri/src/ffmpeg/mod.rs tests  
- **Effort:** small · **User-facing:** no

**Why it matters.** On this machine the '13/13 export formats' and '8/8 containers' Rust tests are genuine. On any other machine, or after a lost bin/, they would silently measure a gpl-master ffmpeg with a different encoder set and the SKIP message would never print. Minor today, a trap for the next environment.

**Evidence.**

```
src-tauri/src/ffmpeg/mod.rs locate_binary(): checks exe-dir, then Path::new("bin").join(...), then '// Fallback to PATH  Ok(base.to_string())' -- no Err path. Guards that depend on it: line 1840 'let Ok(ffmpeg) = ffmpeg_binary() else { eprintln!("SKIP every_offered_video_container_decodes: no ffmpeg binary..."); return; }', 2026-2028, 2102-2105, 2159-2164 ('The binaries are gitignored, so a fresh clone legitimately has none. Say so loudly'). 'where.exe ffmpeg' -> C:\ffmpeg-gpl\ffmpeg-master-latest-win64-gpl\bin\ffmpeg.exe. Shipped build per src-tauri/bin/SIDECARS.json is FFmpeg 8.0-essentials. Today src-tauri/bin/ffmpeg-x86_64-pc-windows-msvc.exe exists (98,740,792 bytes), so cargo test (cwd src-tauri) does hit the bundled one.
```

### Keyframe easing is always linear; no UI to change it

- **Area:** timeline / keyframes  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** The transport strip now shows keyframe diamonds (HEAD), inviting keyframing that can only ever interpolate linearly.

**Evidence.**

```
src/components/EffectStack/ParameterPanel.tsx:492 `easing: "linear"` hardcoded on keyframe creation; store supports hold/linear/easeIn/easeOut/easeInOut (src/store/e2e.test.ts:253-257 applyEasing); the only non-linear producer is AudioPanel/index.tsx:99 (easeInOut, beat keyframes). PRODUCT-REVIEW.md:117-119 'Still to decide'.
```

### Updater is disabled but its plugin dependency, capability grant and check_update command are still compiled in

- **Area:** updater  
- **Effort:** small · **User-facing:** no

**Why it matters.** Dead IPC surface and a capability for a plugin that is not loaded; the 'decide before any public release' note (handoff line 198) is answered by omission.

**Evidence.**

```
tauri.conf.json has no `plugins` key; src-tauri/src/lib.rs:79-82 'The updater plugin is NOT registered'; Toolbar.tsx:593-599 menu item removed. Leftovers: src-tauri/Cargo.toml:56 tauri-plugin-updater = "2.0.0"; src-tauri/capabilities/default.json:32 "updater:default"; lib.rs:20 check_update still in the invoke list. Handoff checklist item 5 leaves re-enabling 'optional'.
```

---

## UNKNOWN (8)

### Cancelling a datamosh kills only the direct child; the mosh-cli process tree may survive

- **Area:** Datamosh cancel  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** A cancelled export that keeps encoding in the background at full CPU is invisible to a non-technical user and competes with the next export.

**Evidence.**

```
commands.rs:2421-2423 `child.kill()` then `wait_timeout(10s)`; no Windows job object anywhere (`grep -n "JobObject|kill_on_drop" src-tauri/src/commands.rs` -> nothing). mosh-cli is a PyInstaller `--onefile` bundle (scripts/build-mosh-sidecar.py:137) whose Python spawns ffmpeg/ffgac/ffedit itself (mosh_cli.py:96-140). Needs a Task Manager check after cancelling a long mosh to confirm whether ffmpeg keeps running and whether a `_MEI` dir is left (the 2.3 MB `_MEI0000e5fc2` in %TEMP% is consistent with a killed small bundle).
```

### SAM3 bridge pins ~3.4 GB of non-pageable RAM for its whole lifetime, a candidate contributor to the commit-limit export crash

- **Area:** Export crash lead  
- **Effort:** medium · **User-facing:** yes

**Why it matters.** docs/SESSION-HANDOFF.md:112-117 names the 107 GB / 110 GB commit limit as the current theory for the 0xc0000374 crash; a session that touched SAM3 adds several GB of unpageable commit before export allocates.

**Evidence.**

```
sam3_bridge.py:399-408: when DEVICE == "cuda" every parameter and buffer is `.pin_memory()`'d after load; ModelManager (411) offloads to CPU after 30 s idle but the process and its pinned host memory stay resident until app close (lib.rs:86-96). Page-locked memory counts against the Windows commit limit and cannot be paged out. Not measured here; test: reproduce the export with SAM3 never started in the session versus started once.
```

### SAM License link in the setup panel may not open (no opener/shell plugin)

- **Area:** SAM3 setup panel  
- **Effort:** small · **User-facing:** yes

**Why it matters.** The licence checkbox is the legal gate before a 6.5 GB download; if the link is dead the user is asked to accept a document they cannot open (the text is bundled under licenses/, tauri.conf.json:68, but nothing in the panel opens it either).

**Evidence.**

```
Sam3Setup.tsx:133-139 `<a href="https://huggingface.co/facebook/sam3.1" target="_blank">`. `grep -n "opener|plugin-shell" src-tauri/Cargo.toml package.json` -> nothing; CHANGELOG notes tauri-plugin-shell was removed. Whether WebView2 opens a bare popup or nothing at all needs a click in the built app to confirm.
```

### The recorded crash code (0xc0000374 STATUS_HEAP_CORRUPTION) is not what the commit-limit theory predicts; the theory is unverified and the crash handler cannot see it

- **Area:** export crash diagnosis  
- **Effort:** human-step · **User-facing:** yes

**Why it matters.** If the crash is real heap corruption it will follow users onto machines with plenty of memory, and raising the owner's pagefile will hide it locally. What would settle it: the Event Log record's faulting module+offset, a WER LocalDumps .dmp for moshdither-studio.exe, and the exact stack/format/source of the failing export, reproduced once with ComfyUI closed.

**Evidence.**

```
docs/PRODUCT-REVIEW.md:190-193: Windows records `0xc0000374 (STATUS_HEAP_CORRUPTION)` in ntdll and notes it is a fast-fail that bypasses the app's filter; docs/SESSION-HANDOFF.md:112-117 offers the commit limit as the lead. A failed Rust allocation goes through `handle_alloc_error` -> abort (fast-fail 0xC0000409, 'memory allocation of N bytes failed' on a stderr that release builds discard), not through heap validation; 0xC0000374 is raised when ntdll finds the heap's own metadata inconsistent. In-process native code that could do that: WebView2/wry, the `image` 0.25.10 codecs, sysinfo, the AppBar `unsafe` in src-tauri/src/window_commands.rs:256-315 and the exception filter in src-tauri/src/crash.rs:222-256. crash.rs:224-231 does not name 0xC0000374 or 0xC0000409 either way. Nothing in the tree records where in the export the fault occurred.
```

### Export crash 0xc0000374 still unreproduced; the commit-limit theory is consistent with this machine right now

- **Area:** export stability  
- **Effort:** human-step · **User-facing:** yes

**Why it matters.** If it is an app heap bug rather than the host's commit limit, it ships to users who cannot diagnose it. The handoff's own test ('export with ComfyUI closed') has not been recorded as run.

**Evidence.**

```
docs/PRODUCT-REVIEW.md:188-204 'Not explained'; docs/SESSION-HANDOFF.md:111-117. Measured now via Get-CimInstance Win32_OperatingSystem: CommitLimit 109.7 GB, CommitFree 19.4 GB (~90 GB charged), PhysTotal 93.7 GB, PhysFree 44 GB; Win32_PageFileUsage: C:\pagefile.sys AllocatedBaseSize 16384 MB. Nothing in the tree changed for this item (no repro recipe, no pagefile guidance in README).
```

### Claim that the 2.9 GB sidecar carries its dependencies' licences is unverified; CUDA/cuDNN redistribution terms are not mentioned

- **Area:** licensing / SAM3 sidecar  
- **Effort:** human-step · **User-facing:** no

**Why it matters.** If the bundle lacks the licence files, the notices document is wrong for the largest thing the app distributes; NVIDIA runtime redistribution has its own notice requirement.

**Evidence.**

```
THIRD-PARTY-NOTICES.md:123-126 "These carry their own licences ... which are included inside the bundle as distributed by their respective projects." `grep -n copy_metadata|collect_all|LICENSE scripts/build-sam3-sidecar.py` -> no matches (PyInstaller does not collect dist-info LICENSE files unless asked). src-tauri/src/sam3_addon.rs:6-7 describes the sidecar as "PyInstaller bundle of PyTorch + CUDA"; no NVIDIA EULA or cuDNN licence in licenses/ (`wc -c licenses/*` lists only GPL-2.0, GPL-3.0, INSTALLER-LICENSE, SAM-LICENSE, THIRD-PARTY-NOTICES, two BUILD-INFO files). To verify: unpack the built sidecar and list `*.dist-info/LICENSE*` and `nvidia_*` packages.
```

### Playwright reuses any dev server already on port 1420, and three checkouts of this repo share that port -- the gate could silently test another tree's frontend

- **Area:** playwright.config.ts / worktree isolation  
- **Effort:** small · **User-facing:** no

**Why it matters.** If 'npm run dev' happens to be running from master or the Windsurf worktree, this worktree's E2E gate passes or fails on THEIR code with no indication. Would need: check which tree served each logged run (not recoverable from the logs). One-line fix: reuseExistingServer: false.

**Evidence.**

```
playwright.config.ts: 'reuseExistingServer: !process.env.CI' and webServer 'url: http://localhost:1420'. vite.config.ts: 'port: 1420, strictPort: true'. git worktree list: C:/Users/richk/CascadeProjects/moshdither-studio [master], C:/Users/richk/.windsurf/worktrees/moshdither-studio/moshdither-studio-4bb0d5ff [4bb0d5ff], this worktree [Cosmic/upbeat-golick-68081b]. netstat at audit time: nothing bound to :1420. I could not verify whether any logged run reused a foreign server.
```

### 'SAM3 works in an installed app' has only been exercised with a hand-placed add-on and there is currently no installed copy of the app on this machine to confirm the 0.2.0 build was run

- **Area:** release mechanics — verification of the shipped path  
- **Effort:** human-step · **User-facing:** yes

**Why it matters.** The user journey that matters for release — run the 0.2.0 installer on a clean machine, launch, datamosh, then download SAM3 through the published manifest — has not been performed end to end. To resolve: install the 0.2.0 NSIS on a clean VM (no ~/.moshdither, no Python), verify launch and a datamosh export, then repeat the SAM3 download after the add-on is published.

**Evidence.**

```
`ls ~/.moshdither/sam3/` -> sam3-bridge.exe 3,061,917,014 B dated 2026-09-09 12:14:45 (placed before any release exists, so not via the download path); `ls ~/.moshdither/models/sam3/` -> sam3.pt 3,450,062,241 B dated 2026-06-14. SESSION-HANDOFF.md:47 "SAM3 now works in an installed app (as of 2026-09-08)" and :20 points at `$env:LOCALAPPDATA\MoshDither Studio\moshdither-studio.exe`; `ls "/c/Users/richk/AppData/Local/MoshDither Studio/moshdither-studio.exe"` -> No such file; `ls "/c/Program Files/MoshDither Studio/"` -> No such file. Gate 10 ('Built app starts and stays up', 20.3 s) exercises target/release/moshdither-studio.exe, not the installer.
```

---

## What the audit found SOLID

- 'Check for Updates' is deliberately absent with a comment explaining why (Toolbar.tsx:598-606), matching the dead-updater note in SESSION-HANDOFF, rather than shipping a button that can only fail.
- A panic inside export, load, ffglitch or the add-on cannot take the app down: heavy work runs under `tauri::async_runtime::spawn_blocking` and a JoinError is mapped to `Err("Export task failed: ...")` / `"Task failed: ..."` (commands.rs:719-746, 129-160, 2760-2775, 2828-2834, 2850-2857). The panic hook, per-run log file (newest 20 kept) and Windows exception filter are installed before `run()` (main.rs:12-14, crash.rs:145-256), and `get_log_path` is exposed to the UI (crash.rs:104-107).
- Add-on install trust boundary is correct: parts are streamed to disk and hashed as they arrive (sam3_addon.rs:197-243), the sidecar hash is compiled in and checked before any byte is downloaded (:288-301, :337), assembly happens in staging and is renamed into place only after verification (:401-407, :447-449), so an interrupted run can never leave a half-written executable where the engine looks.
- Add-on trust boundary is right and tested: EXPECTED_SIDECAR_SHA256 compiled in (sam3_addon.rs:51-52), refused before any byte moves (349, check_offered_sidecar 285-297), per-part hash with removal on mismatch (237-244), whole-file hash then rename-into-place (260-277, 400-404) so a half-written executable is never where the engine looks; tests at 466-635 cover hash shape, manifest parsing, tampered assembly and a real-socket download.
- All five core sidecars are present in src-tauri/bin with tauri.conf.json externalBin listing exactly them (ffmpeg, ffprobe, ffgac, ffedit, mosh-cli); sam3-bridge is deliberately NOT in externalBin of tauri.conf.json or tauri.release.conf.json, so the 2.9 GB file in bin/ cannot leak into an installer.
- All five sidecars are declared and actually bundled: tauri.conf.json externalBin lists bin/ffmpeg, bin/ffprobe, bin/ffgac, bin/ffedit, bin/mosh-cli; scripts/enable-sam3-sidecar.mjs:63-78 rebuilds externalBin/resources additively from the base config (so the overlay cannot drop them); the 7z listing of the 0.2.0 NSIS shows ffmpeg.exe, ffprobe.exe, ffgac.exe, ffedit.exe and mosh-cli.exe present with sizes matching src-tauri/bin/.
- Asset-protocol scope is as narrow as it can be and the code respects it: tauri.conf.json:35-44 allows only $TEMP/moshdither-studio/**, $APPDATA/com.moshdither.studio/**, $APPCONFIG/com.moshdither.studio/**; user media is never served by path (load_media reads in Rust and returns pixels), and proxies, previews, extracted audio and custom-LUT copies are written under $TEMP/moshdither-studio precisely so convertFileSrc works (ffmpeg/mod.rs:207-210, commands.rs:2178-2212). CSP (tauri.conf.json:31) has no unsafe-eval, script-src inherits 'self', connect-src is self+asset only; capabilities/default.json grants only core window ops and dialog; dragDropEnabled:false.
- Both logged full-gate runs show the Rust side genuinely compiling and testing under -D warnings (gate 5 'Rust lints (-D warnings) pass', gate 6 'Rust tests pass 14.2s/13.6s') and the front-end production build passing -- the tree as committed type-checks, lints, and builds.
- Bundled binaries are what the manifest says they are: `sha256sum` of src-tauri/bin/ffmpeg and ffprobe -> d4de4dda... and d1ae1654..., matching src-tauri/bin/SIDECARS.json; fetch-external-bins.mjs re-obtains pinned versions with hash verification.
- Claimed duration fix is in the tree: src/components/AppLayout.tsx:176-182 setDuration(meta.duration) after the probe.
- Claimed effect-waiting banner exists: src/utils/effectConverter.ts:1076-1085 REQUIRES_SELECTION covers composite.overlay and color.lut_grading.
- Claimed parameter-range fixes are in the tree: src/components/EffectStack/ParameterPanel.tsx:145 range-dependent precision, :327-329 and :359-360 min/max/step on the number box; src-tauri/src/effects/params.rs:270-348 slider_range_tests (every_slider_has_a_usable_range, defaults_at_range_ends_are_accounted_for).
- Composition guides are explicitly preview-only and say so in their tooltips (PreviewViewport.tsx:1275-1300); custom LUTs go through the native file dialog and are copied into an allowed dir before preview (LUTPanel/index.tsx:16-42).
- Console-window guard covers every spawn in the tree: `grep -rn 'Command::new|tokio::process|.sidecar(|.shell()|proc::command(' src-tauri/src` shows all 28 live spawn sites use `crate::proc::command` (commands.rs:2348,2482,2644,2968-2989; environment.rs:78,240; ffmpeg/mod.rs:221,391,525,1268,1575,1665,1760,1793,1868,2061; sam3_engine.rs:357,378,410); the only bare `std::process::Command::new` calls are in effects/functional_tests.rs:504,538,591 (allowlisted CLI/test code). Python spawns at packages/python-backend/mosh_cli.py:129 and DatamoshLib/FFG_effects/basic_modes.py:79 pass `**_NO_WINDOW`. scripts/check-no-console-windows.mjs is wired into `prebuild` (package.json:9), which the gate's build step runs (evals/regression.ps1:40). No tauri-plugin-shell or tokio::process usage exists to slip past it.
- Console-window suppression is real in both languages: the only Command::new outside src-tauri/src/proc.rs are in effects/functional_tests.rs (tests); packages/python-backend/mosh_cli.py:39-42 and DatamoshLib/FFG_effects/basic_modes.py:14-17,80 pass creationflags 0x08000000; scripts/check-no-console-windows.mjs runs in package.json prebuild.
- Crash diagnostics exist and work: crash.rs writes one log per run under ~/.moshdither/logs, keeps newest 20 (verified: exactly 20 `moshdither-*.log` files present), installs a panic hook and a Windows SEH filter, and exposes the path via get_log_path; main.rs logs version + git SHA first.
- Crash recovery is genuinely in place: autosave every 5 s to localStorage with a quota fallback that drops the media data URL rather than the session (useProjectSession.ts:38-52, :96-121); restore drops a moved media path instead of leaving export to fail later (:172-186) and reports an unrestored preview honestly (:200-204); close goes through Tauri onCloseRequested, not just beforeunload (AppLayout.tsx:81-140). A 0xc0000374 crash mid-export loses at most 5 s of stack/keyframe edits.
- Datamosh mode list is honest: ExportPanel/index.tsx FFGITCH_MODES = 25 verified ids, grouped; the seven zero-byte tomato modes are excluded with the reason written next to the list.
- Datamosh modes are previewable before export: preview_ffglitch trims a re-encoded 2 s segment and caches by (source, mode, start, length) (commands.rs:2598-2680); the dropdown lists only the 25 modes verified to produce output, grouped, with the broken tomato family and second-input modes deliberately excluded and documented (ExportPanel/index.tsx:72-118).
- Effect quality evidence is recent and reproducible: evals/reports/quality-audit-real-footage.md (2026-09-09T15:57) shows 99/99 still, 69 video, 0 calibration flags, 0 category-claim violations, 8/8 audio-reactive responding; parameter-range-audit.md covers all 186 parameters.
- Empty and first-run states are present everywhere a user lands: preview dropzone 'Drop an image or video here to begin' (PreviewViewport.tsx:1344-1360), 'Empty stack' (EffectStack/index.tsx:117-121), 'No saved presets' (PresetPanel:170-173), 'Load media to use SAM3 segmentation' (MaskPanel:262-268), 'No tracks' (TrackPanel:45-49); onboarding carries a photosensitivity advisory (OnboardingModal.tsx:79-86).
- Encode is atomic and self-checking: it writes to a hidden sibling temp file and renames only after a clean exit (ffmpeg/mod.rs:988-1000, `TempFileGuard` :833), refuses with a named frame if an effect changes frame size mid-stream (:1322-1345), pads odd dimensions (:1240-1258), and reports FFmpeg's own stderr instead of `os error 109` on early exit (:1347-1367).
- Every docked panel is wrapped in its own ErrorBoundary with a 'Try again' reset so one panel crashing does not take the app down (DockLayout.tsx:111-140); saved layouts are versioned and discarded when incompatible (DockLayout.tsx:29-44).
- Every subprocess wait is bounded (except the mosh-preview trim noted above): probe 30 s (ffmpeg/mod.rs:1573), decode 120 s (:550), audio extract 600 s (:1803), encode 1800 s (commands.rs:1063), ffglitch 3600 s with a 250 ms cancel poll (:2478-2496, 2419-2440), SAM3 handshake 300 s and per-command 30-600 s (sam3_engine.rs:527-531, 588-596). stdout/stderr are drained on threads so a chatty child cannot deadlock the pipe (ffmpeg/mod.rs:1279-1292, 1478-1502; commands.rs:2392-2412).
- Export Video main path is fully wired end to end: format, quality, fps, resolution, processing scale, codec, watermark, in/out trim, audio bake and include-audio all reach exportVideo (ExportPanel/index.tsx:268-285) and the Rust output_spec honours every format id (src-tauri/src/ffmpeg/mod.rs:885-935); the Save dialog's extension wins over the chip (lib/tauri.ts:434-439).
- Export cancel actually reaches the backend (ExportPanel handleCancel -> cancelExport, lib/tauri.ts:595-597) and the two-stage FFglitch export cleans its intermediate via a backend that refuses anything but its own .moshdither-fx-tmp files (lib/tauri.ts:580-588).
- Export concurrency is serialised: a one-permit `tokio::sync::Semaphore` (commands.rs:86) is acquired before the shared cancel flag is reset in both `export_video` (:711-717) and `apply_ffglitch` (:2748-2755), with the ordering documented, so two jobs cannot un-cancel each other.
- Export intermediates are handled carefully: the two-stage temp lives beside the output with a distinctive marker and is removed on both paths (ExportPanel/index.tsx:377, :415); remove_export_temp cannot delete anything without that marker (commands.rs:2575); the ffglitch temp config and failed preview outputs are removed (commands.rs:2778, :2788, :2804). FFmpeg itself refuses output==input, so exporting over the source cannot truncate it.
- First-run empty state is clear and honest: PreviewViewport.tsx:1345-1360 'Drop an image or video here to begin — or click to browse —' with the supported format list; OnboardingModal.tsx:79-86 carries a photosensitivity advisory; MaskPanel.tsx:262-267 says 'Load media to use SAM3 segmentation.'; Sam3Setup.tsx:104-145 discloses the download, shows install state per file, and gates the fetch on licence acceptance.
- Frontend rejections are surfaced on every async entry point I traced: export (ExportPanel/index.tsx:291-301), ffglitch export (:411-416), mosh preview (:338-345), SAM3 init/load/prompts/auto-mask/post-process (MaskPanel.tsx:90-92, 117-124, 144-145, 166-167, 196-197, 219-220, 256-257; PreviewViewport.tsx:476, 499), add-on install (Sam3Setup.tsx:85-89), save/open project (useProject.ts:62-66, 142-145), CPU preview render (PreviewViewport.tsx:1060-1065). Global `error` and `unhandledrejection` handlers forward to the Rust log (main.tsx:18-29) and an ErrorBoundary wraps the app (App.tsx:9). `grep -rn 'void [a-zA-Z_.]+(' src` without `.catch` only finds window-control calls and `refresh()`/`sync()` wrappers that catch internally.
- GPL conveyance paperwork is correct as far as it goes: GPL-2.0 and GPL-3.0 texts in licenses/, BUILD-INFO files generated from the exact shipped binaries on 2026-09-09 whose version strings match the SHA-256 pins in src-tauri/bin/SIDECARS.json (8.0-essentials_build-www.gyan.dev, ffglitch-0.10.2), licenses/ bundled as a resource and INSTALLER-LICENSE.txt wired as bundle.licenseFile (tauri.conf.json:68-70); THIRD-PARTY-NOTICES.md and licenses/THIRD-PARTY-NOTICES.md are byte-identical (diff -> IDENTICAL).
- Indexing on user-controlled data is guarded where it matters: `blend_mask` validates frame/mask dimensions and mask length before any index (effects/engine.rs:18-32); `parts[0]/parts[1]` in commands.rs:779-782 and ffmpeg/mod.rs:1546-1547 are behind `len() == 2`; `segment.frames[0]` at commands.rs:874 is behind `len() == 1` and at ffmpeg/mod.rs:1043 behind the `is_empty()` check at :984; `as usize` casts saturate. `image` 0.25.10's default `Limits` (max_alloc 512 MiB, image-0.25.10/src/io/limits.rs:49-55) bound still-image decodes in `load_image`/`load_image_from_memory` so a giant PNG errors instead of allocating unbounded.
- Licence texts ship with the installer: tauri.conf.json resources `../licenses` -> `licenses` and `licenseFile: ../licenses/INSTALLER-LICENSE.txt`; 7z listing shows licenses\GPL-2.0.txt, GPL-3.0.txt, INSTALLER-LICENSE.txt, SAM-LICENSE.txt, THIRD-PARTY-NOTICES.md, ffglitch-BUILD-INFO.txt, ffmpeg-BUILD-INFO.txt; `diff -q THIRD-PARTY-NOTICES.md licenses/THIRD-PARTY-NOTICES.md` -> IDENTICAL.
- Licence texts that the notices file points at all exist and are bundled: licenses/GPL-2.0.txt, GPL-3.0.txt, SAM-LICENSE.txt, INSTALLER-LICENSE.txt, ffmpeg-BUILD-INFO.txt, ffglitch-BUILD-INFO.txt; tauri.conf.json resources maps ../licenses -> licenses and licenseFile -> INSTALLER-LICENSE.txt; Sam3Setup.tsx:21-33,125-126 gates the SAM download behind an explicit licence-acceptance checkbox.
- Licensing groundwork is in place: licenses/ holds GPL-2.0, GPL-3.0, SAM-LICENSE and build-info files; THIRD-PARTY-NOTICES.md names each redistributed component and its source; publish-sam3-addon.mjs:181-187 refuses to publish the weights without the SAM licence file; the installer shows licenses/INSTALLER-LICENSE.txt (tauri.conf.json:70).
- Mask panel does not offer the 6 GB download on an unanswered probe: MaskPanel.tsx:23-32 treats a failed status probe as 'installed' rather than flashing the setup panel.
- New IPC surface is registered: lib.rs:138-139 apply_ffglitch/preview_ffglitch, :154-155 sam3_addon_status/sam3_addon_install, :20 animate_still_as_video.
- No console windows: proc.rs:240-248 CREATE_NO_WINDOW wrapper used for every spawn, mosh_cli.py:42-44 sets creationflags for its own ffmpeg children, and scripts/check-no-console-windows.mjs runs in prebuild (package.json:9).
- No panic-able call on a command path outside tests: every `unwrap`/`expect`/`panic!` hit in commands.rs, ffmpeg/mod.rs, sam3_engine.rs, sam3_addon.rs, effects/*.rs, dsp/, audio/, utils/ sits after the file's `#[cfg(test)]` line, except commands.rs:381 (`preview_scale.unwrap()` guarded by `scaled` requiring `Some`), commands.rs:1202 (`sam3_lock.as_ref().unwrap()` immediately after it is set at :1197) and sam3_addon.rs:411 (`expect("checked above")` guarded by `need_ckpt` requiring `checkpoint.is_some()` at :331). No `todo!`/`unimplemented!`/`unreachable!` outside tests. Cargo.toml has no `panic = "abort"` override (`grep -n 'panic|\[profile' src-tauri/Cargo.toml` -> only a comment at :65).
- No secrets in the tree or history: only .env.example is tracked and it holds placeholders (`GH_TOKEN=ghp_xxxx...`); grep for hf_/sk-/ghp_/AKIA/api_key patterns finds nothing real; `git log --all --diff-filter=A` shows no committed .env/.pem/.pfx/.p12/credentials outside an SDK example in node_modules; core.hooksPath=.husky and .husky/pre-commit runs `npm run secret-scan` on every commit; .pre-commit-config.yaml and .secrets.baseline exist.
- Only ONE #[ignore] in the entire Rust tree and it carries a reason: src-tauri/src/ffmpeg/mod.rs:2100 '#[ignore = "memory-heavy; run explicitly with --ignored"]' on large_frames_export_without_corrupting_the_heap. Nothing is quietly parked.
- Path validation is real and applied everywhere I traced: src-tauri/src/path_guard.rs rejects relative paths, UNC/verbatim-UNC prefixes, normalises `..` then canonicalises, requires an existing parent for outputs, and blocks Windows/Program Files/ProgramData/$Recycle.Bin (with TEMP/APPDATA/USERPROFILE allow-roots); unit-tested (path_guard.rs:220-280). Every user-path command calls it: load_media (commands.rs:130), save_media (:563), save_processed_image (:616), export_video source/output/watermark image/font (:674-698), get_media_metadata (:1096), extract_audio (:1116), prepare_custom_lut (:2185), remove_export_temp (:2570 plus the `.moshdither-fx-tmp.` marker check at :2575), preview_ffglitch (:2605), apply_ffglitch (:2690-2695 incl. validate_ffglitch_extra_paths), generate_proxy (:2825), animate_still (:2846). Project read/write additionally restricts to .moshdither/.json, refuses dotfiles, caps at 10 MB (commands.rs:2101-2170) with tests at :2860-2960.
- Release builds are console-free and crash-diagnosable: src-tauri/src/main.rs:1 `#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]`; src-tauri/src/crash.rs provides a per-run log file, panic hook and Windows last-chance exception filter; scripts/check-no-console-windows.mjs runs in `prebuild` (package.json).
- Rust coverage is real and broad, not decorative: 648 #[test] functions across 98 files (grep -rn '#\[test\]' src-tauri/src | wc -l = 648); the export matrix test (src-tauri/src/ffmpeg/mod.rs:2156-2258) actually encodes 13 formats through encode_video and checks for a non-empty file or a numbered sequence; every_offered_video_container_decodes (1839+) generates and reads back 8 containers; commands.rs 1856-2065 pins mask inside/outside/alpha blending end-to-end through decode_mask_b64 -> blend.
- SAM3 add-on trust model is sound: EXPECTED_SIDECAR_SHA256 is compiled in (sam3_addon.rs:52-53), a manifest offering a different hash is refused before any bytes move (:233-247, :302), every part is SHA-256'd while streaming (:194-232), the assembled file is re-hashed, staged and renamed into place (:395-399), and mismatches are deleted; tests cover tampered downloads and manifest substitution (:462-630). The publish script refuses to upload the weights without SAM-LICENSE.txt and uploads it beside them (publish-sam3-addon.mjs:181-186, 211-213).
- SAM3 bridge IPC is hardened: length-prefixed frames with a 512 MiB cap (sam3_engine.rs:476-492), a CSPRNG auth token handshake (:449-455, :495-517), a `command_mutex` serialising send/recv (:544-548), a drain on timeout to avoid pipe desync (:566-573), auto-restart on a broken pipe (commands.rs:1203-1224), stale-bridge reaping by PID with an identity check so an unrelated process is never killed (sam3_engine.rs:100-140), shutdown on window close (lib.rs:86-95) and on Drop (:960-965).
- SAM3 first-run is honest and gated: licence acceptance before download, sizes shown, progress events (Sam3Setup.tsx:99-173); a failed status probe does not offer a 6 GB download (MaskPanel.tsx:26-32); prompts await the image load so 'Model not loaded' races are gone (MaskPanel.tsx:63-97); point/box modes have a Start button instead of a dead hint (MaskPanel.tsx:340-347).
- Save/Open Project are in the File menu with shortcuts, autosave recovery is offered on launch, and the window-close guard uses Tauri's onCloseRequested so it fires in the desktop app (AppLayout.tsx:81-140, 350-404; Toolbar.tsx:551-580).
- Sidecar IPC is authenticated: a random 32-byte token per launch (sam3_engine.rs:459-465) is verified by the bridge with hmac.compare_digest on the handshake and every request (sam3_bridge.py:281-295, :1041).
- Sidecar identity chain is consistent end to end: src-tauri/src/sam3_addon.rs:51-52 EXPECTED_SIDECAR_SHA256 (a1066072...dec9b) == build-archive/sam3-addon/sam3-assets.json sidecar.sha256 == scratchpad/sidecar.sha256 == publish.log; only the upload is missing.
- Stale-bridge reaping cannot kill the wrong process: sam3_engine.rs:128-132 only kills a PID whose exe or command line proves it is the bridge.
- The SAM3 add-on trust boundary is implemented as designed: the staged manifest's sidecar sha256 (build-archive/sam3-addon/sam3-assets.json) equals EXPECTED_SIDECAR_SHA256 at src-tauri/src/sam3_addon.rs:51-52; check_offered_sidecar (:285-295) and join_and_verify (:403) refuse anything else; parts are split at 1,900,000,000 B (publish-sam3-addon.mjs:52) under GitHub's 2 GB cap; the manifest URL is overridable via MOSHDITHER_SAM3_MANIFEST (:154) for testing.
- The SAM3 add-on's trust model is tested where it matters: src-tauri/src/sam3_addon.rs:551-610 downloads through reqwest against a real local TcpListener, asserts progress callbacks fire past 8 MiB, and asserts a tampered body is refused with 'Checksum mismatch' AND removed from disk; 612-625 asserts a manifest cannot substitute a sidecar hash other than the compiled-in EXPECTED_SIDECAR_SHA256.
- The Verify/Diagnostics panel is hidden by default (store/index.ts:588-591) and only reachable under View > Advanced with a plain-language title (Toolbar.tsx:770-781); previewAutoExact has no user-facing toggle, so the preview-mode choice really is gone for users.
- The WebGL preview does get one real pixel-level regression test: tests/e2e/live-preview-param-update.spec.ts screenshots the actual canvas before/after changing Bayer matrix size and requires RMSD > 2.0 via tools/render_parity_auditor.py; tests/e2e/output/param-update-before.png (46,950 B) and -after.png (93,724 B) at 17:43 confirm real pixels were compared in the last gate run.
- The alive-after-20 s half of app-smoke does catch the regression that motivated it: a Tauri PluginInitialization panic during setup exits the process, and regression.ps1:98-102 fails the gate when the process is gone.
- The core app needs no internet and no Python: all five tools ship as externalBin (tauri.conf.json:57-63), LUTs/licences/python-backend as resources (64-69), CSP `font-src 'self'` (31); the only `reqwest::` user is src-tauri/src/sam3_addon.rs (grep), and the two frontend `fetch` calls (src/engine/lut/loader.ts:180, src/hooks/useAutoAudioExtract.ts:92) read local asset: URLs. Datamoshing prefers the bundled mosh-cli sidecar (commands.rs:2699-2703), passes ffmpeg/ffgac/ffedit by env (2489-2491), and mosh_cli.py:17-28 reads those env vars.
- The dead updater cannot take the app down: the plugin is not registered (lib.rs:78-84) and the menu item is removed with an explanation (Toolbar.tsx:592-600), matching README.md:114-121.
- The four FFmpeg/FFglitch binaries are pinned and recoverable on a fresh clone: src-tauri/bin/SIDECARS.json records SHA-256, byte counts, exact archive URLs and member paths; scripts/verify-external-bins.mjs runs in `prebuild` and checks hashes (:144-159); `npm run fetch:external` exists (package.json).
- The gate script is honest about failure: evals/regression.ps1 always prints failing output (lines 153-162), orders gates cheapest-first, has no retry logic, and playwright.config.ts sets retries to 0 locally -- so the FAILs in the logs are real events, not hidden ones.
- The last local gate run passed 10/10 (scratchpad gate_timeline.log, 2026-09-09 17:25:59, including the 408 s Playwright suite and 'Built app starts and stays up'); the prior run's single e2e failure (timeline play/pause locator) was fixed rather than skipped.
- The most recent complete gate run on record passed: gate_timeline.log 2026-09-09 17:25:59 — all 10 gates including Rust tests, Playwright E2E (408 s) and 'Built app starts and stays up'.
- The updater is consistently and safely off: `git diff master -- src-tauri/tauri.conf.json` shows the plugins.updater block removed and createUpdaterArtifacts flipped to false; src-tauri/src/lib.rs:79-84 documents that the plugin is deliberately not registered (it would panic at init with a null config); tauri.release.conf.json and tauri.local.conf.json both set createUpdaterArtifacts false; src/components/Toolbar.tsx:592 states 'Check for Updates' is deliberately not offered — no dead menu entry.
- Transport strip does what it says: real fps from ffprobe (Timeline/index.tsx:58), in/out handles that cannot cross (:94-107), keyframe diamonds click-to-jump / right-click-delete (:337-370), stills-only animation length so a video's probed duration cannot be overwritten (:260-282).
- Transport-strip claims match code: AppLayout.tsx:343-345 mounts <Timeline /> under the workspace, not in the dock; Timeline/index.tsx:154 data-testid="transport-strip"; DockLayout.tsx:30 LAYOUT_VERSION = 3 discards old layouts.
- Updater removal was done the safe way: no plugins key in tauri.conf.json, plugin not registered (lib.rs:79-82), menu item removed (Toolbar.tsx:593-599), createUpdaterArtifacts false in all three configs; gate 10 (app-smoke, regression.ps1:64) exists specifically to catch the panic that this once caused.
- Version is consistently 0.2.0 in src-tauri/tauri.conf.json:3, package.json:4 and src-tauri/Cargo.toml:3, and 0.2.0 NSIS (188,531,067 B) + MSI (219,603,432 B) installers were built from HEAD at 17:54-17:56 on 2026-09-09 (build_v10.log 'Finished 2 bundles'; bundle/nsis and bundle/msi timestamps).
- Version strings agree everywhere: package.json:4 "0.2.0", src-tauri/Cargo.toml:3 version = "0.2.0", src-tauri/tauri.conf.json "version": "0.2.0", CHANGELOG.md:12 `## [0.2.0] - 2026-09-08`, and both installer filenames carry 0.2.0.
- Video-only effects on a still are flagged in the stack with a warning icon and explanatory title (EffectStack/index.tsx:22-27, 198-206), and File > Animate as Video has a modal for length/fps plus a 'Revert to Still' undo (Toolbar.tsx:245-336).
- docs/SESSION-HANDOFF.md:150-168 already enumerates the human release steps accurately (push/PR, publish add-on, attach GPL source, code signing, optional updater) — the findings above agree with that list and add what it omits.
- package-lock.json is tracked (`git ls-files package-lock.json`), so the frontend dependency tree is reproducible even though Cargo.lock is not.
- scripts/check-no-console-windows.mjs runs in npm prebuild (package.json 'prebuild') and statically scans BOTH the Rust spawn sites and the Python (mosh_cli.py, basic_modes.py) spawn sites for missing CREATE_NO_WINDOW -- the class of bug that reached users twice now fails the build.
- tests/e2e/real-backend.spec.ts refuses to run against a stale mosh-verify (lines 84+ compare the binary mtime to the newest .rs) and ships committed 139 KB fixtures (tests/fixtures/test-image.png 16,253 B, test-video.mp4 122,892 B) so its media cases run instead of skipping; its own comment records that the previous '' default made every case skip 'indistinguishable from passing'.
- vitest is hermetic and no longer flakes on this machine: vite.config.ts pins NODE_ENV=test and caps maxWorkers to half the cores with a written rationale; gate 4 passed in both logged runs (7.7 s / 8.4 s) and the suite is 65 files / ~1,169 it()/test() calls, plus tests/scripts/sam3-build.spec.ts covering the release overlay generator.
