# Python Backend Dependencies for MoshDither Studio
# Pinned to compatible-release specifiers to avoid surprise breakages and
# supply-chain drift (see architecture review #19).

# Image processing (used by tools/render_parity_auditor.py)
Pillow>=12.2.0,<13

# SAM 3 (Meta Segment Anything Model 3)
torch>=2.0.0,<3
torchvision>=0.15.0,<1
timm>=1.0.17,<2
numpy>=2.0,<3
tqdm>=4.65.0,<5
ftfy==6.1.1
regex>=2023.0.0
iopath>=0.1.10,<1
huggingface_hub>=0.20.0,<1

# SAM PRO mode post-processing (connected components, morph ops)
opencv-python>=4.8.0,<5

# SAM 3 additional runtime deps (not in core pyproject.toml but required at import)
einops>=0.7.0,<1
triton-windows>=2.1.0; platform_system=="Windows"
pycocotools>=2.0.7,<3
psutil>=5.9.0,<6
scikit-image>=0.21.0,<1
